# HelloPerformance Memory Tracking

## Scope

- This file tracks `HelloPerformance` static RAM, heap, and stack after each RAM-focused change.
- `PFB` is listed for completeness, but it is user-configurable and not an optimization target.
- If a RAM change is `<= 500B`, keep the stricter small-change rule: if the speedup is less than `5%`, prefer the lower-RAM option instead of keeping extra SRAM for a marginal win.
- If a RAM change is `> 500B`, allow up to `10%` performance regression in exchange for the RAM reduction, but record the affected hot scenes here.
- Any buffer whose size follows font size, image size, screen size, or `PFB` size must use `heap`; do not replace that with macro-fixed RAM, static storage, or large stack locals just to keep `heap=0`.
- Size-related heap on HelloPerformance must stay bounded: do not accept whole-image-size resident heap. Allowed upper bound is at most `1 * PFB` bytes, or at most `2` image/screen-sized rows or columns of scratch/cache. If a larger-RAM variant is proposed, it must still obey that ceiling and stay separate from the default low-RAM path.
- If such a size-related buffer must stay resident on heap, record the owner, lifetime, and byte size in this file instead of leaving it implicit.
- For performance-oriented variants, even if a size-related heap buffer delivers `>= 2x` speedup, it still must stay within the HelloPerformance ceiling above; if it exceeds `1 * PFB` or `2` rows/columns, reject it instead of introducing a larger resident-heap path.
- Static RAM numbers use the normal QEMU performance build:
  - `make clean`
  - `make all APP=HelloPerformance PORT=qemu CPU_ARCH=cortex-m3`
  - `llvm-size output/main.elf`
- Heap numbers use the QEMU measurement build:
  - `make clean`
  - `make all APP=HelloPerformance PORT=qemu CPU_ARCH=cortex-m3 USER_CFLAGS="-DQEMU_HEAP_MEASURE=1 -DQEMU_HEAP_ACTIONS_APP_RECORDING=1 -DEGUI_CONFIG_RECORDING_TEST=1"`
  - add `-DQEMU_HEAP_TRACE_ACTIONS=1` when action-level heap attribution is needed
  - `Copy-Item example\HelloPerformance\resource\app_egui_resource_merge.bin output\app_egui_resource_merge.bin -Force`
  - `qemu-system-arm ... -kernel output/main.elf`
- Stack numbers use the QEMU stack-usage build:
  - `make clean`
  - `make all APP=HelloPerformance PORT=qemu CPU_ARCH=cortex-m3 USER_CFLAGS="-fstack-usage"`
  - summarize `output/obj/HelloPerformance_qemu/**/*.su`
- `-fstack-usage` reports per-function stack frame size, not the full runtime call-chain peak.

## History

| Date | Change | text | data | bss | static RAM | Heap | Max stack frame | Notes |
| --- | --- | ---: | ---: | ---: | ---: | --- | ---: | --- |
| 2026-03-27 | Alpha change tables moved to rodata | 2172668 | 52 | 22132 | 22184 | 0 / 0 | n/a | Baseline before this round |
| 2026-03-27 | Disable HelloPerformance RLE checkpoint fallback state | 2172576 | 52 | 22116 | 22168 | 0 / 0 | n/a | `static RAM -16B`, `rle_checkpoint` removed |
| 2026-03-27 | Disable HelloPerformance touch pipeline | 2168220 | 52 | 22036 | 22088 | 0 / 0 | n/a | `static RAM -80B`, removed unused touch/input state for timer-driven recording |
| 2026-03-27 | Compact HelloPerformance QOI RGB565 index | 2168836 | 52 | 21972 | 22024 | 0 / 0 | n/a | `static RAM -64B`, `qoi_state 276B -> 212B`, text `+616B` |
| 2026-03-27 | Tighten HelloPerformance text transform stack bounds | 2166116 | 52 | 21972 | 22024 | 0 / 0 | 4456 | `text -2720B`, rotated-text top frames `6248/4888 -> 4456/3416`, heap stays `0` |
| 2026-03-27 | Shrink HelloPerformance external text transform stack buffers | 2166120 | 52 | 21972 | 22024 | 0 / 0 | 4456 | `text +4B`, `egui_canvas_draw_text_transform 3416 -> 2936`, external glyph helpers `856/864 -> 392/400`, heap stays `0` |
| 2026-03-27 | Shrink HelloPerformance shadow corner stack LUT | 2166120 | 52 | 21972 | 22024 | 0 / 0 | 4456 | `egui_shadow_draw_corner 1048 -> 792`, `SHADOW_ROUND 7.158 -> 6.113 (-14.6%)`, heap stays `0` |
| 2026-03-27 | Refactor HelloPerformance ellipse outline stack frame | 2162556 | 52 | 21972 | 22024 | 0 / 0 | 4456 | `text -3564B`, `egui_canvas_draw_ellipse 1048 -> 360`, helper frames `40/144`, `ELLIPSE 2.253 ms`, heap stays `0` |
| 2026-03-27 | Refactor HelloPerformance line HQ polyline stack frame | 2162556 | 52 | 21972 | 22024 | 0 / 0 | 4456 | `line_hq_draw_polyline_internal 1016 -> 152`, new `line_hq_draw_polyline_segment 976`, `LINE_HQ 3.557 ms`, heap stays `0` |
| 2026-03-27 | Reduce HelloPerformance shadow d_sq LUT cap to 96 | 2162556 | 52 | 21972 | 22024 | 0 / 0 | 4456 | `EGUI_CONFIG_SHADOW_DSQ_LUT_MAX 128 -> 96`, `egui_shadow_draw_corner 792 -> 728`, `SHADOW_ROUND 6.113 -> 5.584 (-8.7%)`, heap stays `0` |
| 2026-03-27 | Reduce HelloPerformance shadow d_sq LUT cap to 64 | 2162556 | 52 | 21972 | 22024 | 0 / 0 | 4456 | `EGUI_CONFIG_SHADOW_DSQ_LUT_MAX 96 -> 64`, `egui_shadow_draw_corner 728 -> 664`, `SHADOW_ROUND 5.584 -> 5.584`, same heap `0` |
| 2026-03-27 | Remove HelloPerformance round-rect gradient cache LUT | 2162492 | 52 | 21972 | 22024 | 0 / 0 | 4456 | `text -64B`, `egui_canvas_draw_round_rectangle_corners_fill_gradient 992 -> 472`, `GRADIENT_ROUND_RECT 3.766 -> 3.766`, `GRADIENT_ROUND_RECT_CORNERS 2.235 -> 2.235`, heap stays `0` |
| 2026-03-27 | Shrink HelloPerformance circle gradient cache LUT | 2162644 | 52 | 21972 | 22024 | 0 / 0 | 4456 | `text +152B`, `color_cache 256 -> 129`, `egui_canvas_draw_circle_fill_gradient 1000 -> 752`, `GRADIENT_CIRCLE 10.428 -> 9.802 (-6.0%)`, heap stays `0` |
| 2026-03-27 | Shrink HelloPerformance rectangle gradient cache LUT | 2162756 | 52 | 21972 | 22024 | 0 / 0 | 4456 | `text +112B`, `color_cache 256 -> 129`, `egui_canvas_draw_rectangle_fill_gradient 792 -> 528`, `GRADIENT_RADIAL 7.080 -> 6.727 (-5.0%)`, `GRADIENT_ANGULAR 7.427 -> 6.914 (-6.9%)`, heap stays `0` |
| 2026-03-27 | Shrink HelloPerformance pfb manager metadata arrays | 2162672 | 52 | 21932 | 21984 | 0 / 0 | 4456 | `static RAM -40B`, `egui_core 228 -> 192`, perf stays in noise, `frame_0171.png` / `frame_0175.png` confirm round-rect mask scene still correct |
| 2026-03-27 | Move visible alpha8 tile scratch to heap with QEMU self-managed allocator | 2162144 | 52 | 21964 | 22016 | 0 / 4096 | 2936 | `text -528B`, `static RAM +32B`, `text_transform_draw_visible_alpha8_tile_layout 4456 -> 104`, QEMU heap uses runtime RAM gap instead of libc/newlib |
| 2026-03-27 | Move codec row-band decode cache from `.bss` to frame heap | 2163132 | 52 | 10940 | 10992 | 0 / 11520 | 2936 | `static RAM -11024B`, row-band pixel/alpha cache now follows image width + row count on heap, `HelloPerformance` runtime screenshots `223/223` unchanged, only older saved perf snapshot (`git 789827a`) outlier is `IMAGE_TILED_QOI_565_0 0.888 -> 1.270 (+43.0%)` |
| 2026-03-27 | Move single-row decode scratch from `.bss` to frame heap | 2163368 | 52 | 10468 | 10520 | 0 / 11520 | 2936 | `static RAM -472B`, single-row pixel/opaque-alpha decode scratch now follows screen/image row width on heap, `HelloPerformance` runtime screenshots `223/223` unchanged with `changed=0`, no new `>5%` perf regression |
| 2026-03-27 | Localize HelloPerformance animation benchmark context | 2163408 | 52 | 10412 | 10464 | 0 / 11520 | 2936 | `static RAM -56B`, removed persistent animation view/interpolator/init flag from `.bss`; animation test stack frames are only `128/128/136/208B`, runtime screenshots stay `223/223` with `changed=0`, no new `>5%` perf regression |
| 2026-03-27 | Pack HelloPerformance QOI row index and drop decode-cache dead metadata | 2163424 | 48 | 10408 | 10456 | 0 / 11520 | 2936 | `static RAM -8B`, `qoi_state 212 -> 208`, `egui_image_decode_cache_state 12 -> 8`; heap peak stays `11520B`, runtime screenshots stay `223/223` with `changed=0`, no new `>5%` perf regression |
| 2026-03-27 | Drop HelloPerformance dead root-group touch flag and core PFB size cache | 2163452 | 48 | 10392 | 10440 | 0 / 11520 | 2936 | `static RAM -16B`, `egui_core 192 -> 180`; `touch=0` now compiles `egui_view_root_group_t 64 -> 60`, `egui_core` no longer stores `pfb_total_buffer_size`, heap peak stays `11520B`, runtime screenshots stay `223/223` with `changed=0`, no new `>5%` perf regression |
| 2026-03-27 | Trim HelloPerformance canvas dead clip/spec metadata | 2163340 | 48 | 10384 | 10432 | 0 / 11520 | 2936 | `static RAM -8B`, `text -112B`, `canvas_data 44 -> 32`; compile out canvas extra-clip pointer and spec-circle registration state, runtime screenshots stay `223/223` with `changed=0`, heap peak stays `11520B`, no new `>5%` perf regression |
| 2026-03-27 | Compile out default QEMU heap measurement counters | 2163152 | 48 | 10360 | 10408 | 0 / 11520 | 2936 | `static RAM -24B`, `text -188B`; normal HelloPerformance build no longer keeps QEMU heap current/peak accounting in `.bss`, while the `QEMU_HEAP_MEASURE=1` build still reports the same `interaction_total_peak=11520`, runtime screenshots stay `223/223` with `changed=0`, no new `>5%` perf regression |
| 2026-03-28 | Alias HelloPerformance user root to core root group | 2163104 | 48 | 10296 | 10344 | 0 / 11520 | 2936 | `static RAM -64B`, `text -48B`, `egui_core 180 -> 120`; runtime screenshots stay `223/223` with `changed=0`, heap peak stays `11520B`, stack peak stays `2936B`, QEMU perf profile rerun with no hot-path change expected |
| 2026-03-28 | Compile out HelloPerformance text-transform layout heap fallback | 2162384 | 48 | 10280 | 10328 | 0 / 11520 | 2936 | `static RAM -16B`, `text -720B`; compile out dead text-layout fallback metadata for HelloPerformance, runtime screenshots stay `223/223` with `changed=0`, heap peak stays `11520B`, stack peak stays `2936B`, no active-path perf change expected |
| 2026-03-28 | Reuse HelloPerformance codec row-cache handle for single-row scratch | 2162320 | 48 | 10272 | 10320 | 0 / 11520 | 2936 | `static RAM -8B`, `text -64B`; single-row codec pixel scratch now borrows the row-cache pixel heap handle, and current decode capacity metadata compacts to 16-bit under HelloPerformance bounds; runtime screenshots stay `223/223` with `changed=0`, heap peak stays `11520B`, stack peak stays `2936B`, no new `>5%` perf regression |
| 2026-03-28 | Localize HelloPerformance external font desc scratch | 2162420 | 48 | 10256 | 10304 | 0 / 11520 | 2936 | `static RAM -16B`, `text +100B`; removed persistent `g_selected_char_desc`, runtime screenshots stay `223/223` with `changed=0` vs `0ed8fdb`, heap peak stays `11520B`, stack peak stays `2936B`, `python scripts/code_perf_check.py --profile cortex-m3` PASSED |
| 2026-03-28 | Move HelloPerformance external font row/glyph scratch to transient heap | 2162436 | 48 | 10256 | 10304 | 0 / 11520 | 2920 | `text +16B`, static RAM unchanged; external text transform row cache, visible-tile glyph bitmap scratch, and direct external glyph pixel scratch now follow actual glyph size on heap and free after use, runtime screenshots stay `223/223` with `changed=0` vs `e8d5ef8`, heap peak stays `11520B` with alloc/free `716 / 716`, stack peak `2936 -> 2920`, `python scripts/code_perf_check.py --profile cortex-m3` PASSED |
| 2026-03-28 | Move HelloPerformance image resize and round-rect scratch to transient heap | 2160520 | 48 | 10256 | 10304 | 0 / 11616 | 2920 | `text -1916B`, static RAM unchanged; resize `src_x_map` and round-rect fast row cache now follow actual draw width / tile height on heap and free after use, runtime screenshots stay `223/223` with `changed=0` vs `e8d5ef8`, heap peak `11520 -> 11616`, alloc/free `716 / 716 -> 5666 / 5666`, `python scripts/code_perf_check.py --profile cortex-m3 --threshold 5` PASSED |
| 2026-03-28 | Move HelloPerformance circle mask row cache to frame heap | 2159800 | 48 | 10272 | 10320 | 0 / 11616 | 2920 | `text -720B`, `static RAM +16B`; circle-mask `PFB_HEIGHT` row cache moves from `egui_mask_circle_t` to frame heap scratch (`3 * EGUI_CONFIG_PFB_HEIGHT * sizeof(egui_dim_t) = 96B` at current config) and releases at frame end, runtime screenshots stay `223/223` with `changed=0` vs `04cc338`, heap peak stays `11616B` with alloc/free `5683 / 5683`, `python scripts/code_perf_check.py --profile cortex-m3 --threshold 5` PASSED |
| 2026-03-28 | Move HelloPerformance rotated-text layout/tile scratch to transient heap | 2160560 | 48 | 10272 | 10320 | 0 / 11616 | 1200 | `text +760B`, static RAM unchanged; rotated-text layout/tile collectors now allocate by actual glyph/line counts and free at `cleanup`, `egui_canvas_draw_text_transform 2920 -> 760`, heap peak stays `11616B` with alloc/free `5683 / 5683 -> 7403 / 7403`, runtime screenshots stay `223/223` with `changed=0` vs `04cc338`, `python scripts/code_perf_check.py --profile cortex-m3 --threshold 5` PASSED |
| 2026-03-28 | Restore HelloPerformance rotated-text tiny caches and alpha8 fast path | 2160724 | 48 | 10344 | 10392 | 0 / 11616 | 1200 | `static RAM +72B`; restore `g_text_transform_prepare_cache (60B)` and `s_dim_* (12B)` so buffered rotated text stops redoing affine/string measurement every frame, raise `EGUI_CONFIG_TEXT_TRANSFORM_VISIBLE_ALPHA8_MAX_BYTES 4096 -> 5120` but measured heap peak still stays `11616B`; `TEXT_ROTATE_BUFFERED 12.325 -> 9.159 (-25.7%)`, `EXTERN_TEXT_ROTATE_BUFFERED 14.985 -> 10.759 (-28.2%)`, PC screenshots for `TEXT_ROTATE_BUFFERED` and `EXTERN_TEXT_ROTATE_BUFFERED` confirmed rendering unchanged |
| 2026-03-28 | Keep HelloPerformance decode row-band cache and expose A/B override knobs | 2160724 | 48 | 10344 | 10392 | 0 / 11616 | 1200 | Default build unchanged; `app_egui_config.h` now wraps row-cache knobs in `#ifndef` so `USER_CFLAGS` can isolate A/B tests. `row-cache off` experiment drops heap peak to `7520B`, but compressed-image scenes regress catastrophically and the experiment build rises to `data=44`, `bss=13236`, `static RAM=13280`, so the default config must keep row-cache enabled |
| 2026-03-28 | Delay HelloPerformance alpha8 text tile scratch until fallback path | 2160808 | 48 | 10344 | 10392 | 0 / 11616 | 1200 | `text +84B`, static RAM unchanged; buffered rotated-text alpha8 fast path no longer allocates the full tile scratch before it knows fallback is needed. Whole-run heap peak still stays `11616B` because compressed-image row-band cache dominates, but `TEXT_ROTATE_BUFFERED 7220 -> 6032 (-16.5%)` and `EXTERN_TEXT_ROTATE_BUFFERED` now measures `6332B`; runtime screenshots stay `223/223` with `changed=0`, `python scripts/code_perf_check.py --profile cortex-m3 --threshold 5` PASSED |
| 2026-03-28 | Tighten HelloPerformance buffered-text alpha8 heap ceiling | 2160804 | 48 | 10344 | 10392 | 0 / 11616 | 1200 | `text -4B`, static RAM unchanged; external rotated-text row cache now allocates only after the visible-alpha8 fast path actually falls back, and the default `EGUI_CONFIG_TEXT_TRANSFORM_VISIBLE_ALPHA8_MAX_BYTES` is trimmed `5120 -> 4160` after A/B checks showed `4096` exceeds the `5%` perf budget while `4160` stays within it. Whole-run heap peak still stays `11616B`, but `TEXT_ROTATE_BUFFERED 6032 -> 5072 (-15.9%)` and `EXTERN_TEXT_ROTATE_BUFFERED 6332 -> 5342 (-15.6%)`; `python scripts/code_runtime_check.py --app HelloPerformance --keep-screenshots` passed `223/223`, and `python scripts/code_perf_check.py --profile cortex-m3 --threshold 5` PASSED |
| 2026-03-28 | Add HelloPerformance low-RAM codec tail row-cache mode | 2163080 | 52 | 10348 | 10400 | 0 / 10032 | 1200 | `heap peak 11616 -> 10032 (-1584B)`, `static RAM +8B`, `egui_image_decode_cache_state 8 -> 12`; `EGUI_CONFIG_IMAGE_CODEC_TAIL_ROW_CACHE_ENABLE=1` keeps row-cache enabled but stores only later-tile tail columns for QOI/RLE alpha scenes, compressed-image hot cases stay within the `10%` perf budget for `>500B` RAM savings (worst verified case `IMAGE_RLE_565_8 +8.35%`), scene-local heap for `IMAGE_QOI_565_8` / `EXTERN_IMAGE_QOI_565_8` / `IMAGE_RLE_565_8` / `EXTERN_IMAGE_RLE_565_8` drops to `9936B`, runtime `223/223`, unit test `649/649`, `python scripts/code_perf_check.py --profile cortex-m3 --threshold 10` PASSED |
| 2026-03-28 | Cut HelloPerformance QEMU reserved stack to 4KB | 2163080 | 52 | 6252 | 6304 | 0 / 10032 | 1200 | `static RAM -4096B`; app-specific linker override drops `__qemu_min_stack_size__ 0x2000 -> 0x1000`, so `._user_heap_stack 8196 -> 4100` while compiled stack frames stay unchanged and the measured heap peak stays `10032B`; `python scripts/code_runtime_check.py --app HelloPerformance --keep-screenshots` passed `223/223`, unit test `649/649` passed |
| 2026-03-28 | Remove HelloPerformance external whole-image HQ cache path | 2163080 | 52 | 6252 | 6304 | 0 / 10032 | 1200 | Revert the transform-side probe added for external whole-image persistent cache and drop the app-local override knob. New policy: HelloPerformance does not accept whole-image-size resident heap, even as an opt-in HQ path. Keep the default low-RAM row-cache path only; larger variants must still stay within `1 * PFB` or `2` rows/columns of image/screen-sized scratch |
| 2026-03-28 | Shrink HelloPerformance codec first-tile scratch width | 2168304 | 52 | 6252 | 6304 | 0 / 9456 | 1200 | `static RAM unchanged`; QOI/RLE now split the first visible tile decode so the transient scratch only covers the visible segment while later tiles still reuse the tail-row cache. Whole-run heap `10032 -> 9456 (-576B)`, compressed alpha scene-local heap `9936 -> 9360`, verified perf stays within the `>500B => 10%` rule (`IMAGE_QOI_565_8 +0.36%`, `EXTERN_IMAGE_QOI_565_8 +1.98%`, `IMAGE_RLE_565_8 +2.83%`, `EXTERN_IMAGE_RLE_565_8 +1.97%`), runtime `223/223`, unit test `649/649`, `python scripts/code_perf_check.py --profile cortex-m3 --threshold 10` PASSED |
| 2026-03-28 | Trim HelloPerformance buffered-text alpha8 ceiling to 3648B | 2168304 | 52 | 6252 | 6304 | 0 / 9456 | 1200 | `text/static RAM unchanged`; default `EGUI_CONFIG_TEXT_TRANSFORM_VISIBLE_ALPHA8_MAX_BYTES` trims `4160 -> 3648`, whole-run heap peak still stays `9456B` because compressed-image row-cache dominates, but `TEXT_ROTATE_BUFFERED 5072 -> 4560 (-512B)` and `EXTERN_TEXT_ROTATE_BUFFERED 5342 -> 4830 (-512B)`; verified perf stays within the `>500B => 10%` rule (`TEXT_ROTATE_BUFFERED +9.77%`, `EXTERN_TEXT_ROTATE_BUFFERED +8.54%`), runtime `223/223`, unit test `649/649` |
| 2026-03-28 | Trim HelloPerformance buffered-text alpha8 ceiling to 3072B | 2168304 | 52 | 6252 | 6304 | 0 / 9456 | 1200 | `text/static RAM unchanged`; default `EGUI_CONFIG_TEXT_TRANSFORM_VISIBLE_ALPHA8_MAX_BYTES` trims `3648 -> 3072`, whole-run heap peak still stays `9456B` because compressed-image row-cache dominates, but `TEXT_ROTATE_BUFFERED 4560 -> 3984 (-576B)` and `EXTERN_TEXT_ROTATE_BUFFERED 4830 -> 4254 (-576B)`; verified perf stays within the `>500B => 10%` rule (`TEXT_ROTATE_BUFFERED 10.481 -> 11.402 (+8.79%)`, `EXTERN_TEXT_ROTATE_BUFFERED 11.837 -> 12.756 (+7.76%)`), runtime `223/223`, unit test `649/649`; under the 2026-03-28 implementation, `2560B` was still rejected because `TEXT_ROTATE_BUFFERED` reached `11.596ms (+10.6%)` |
| 2026-03-28 | Cut HelloPerformance QEMU reserved stack to 2.5KB | 2168304 | 52 | 4716 | 4768 | 0 / 9456 | 1200 | `static RAM -1536B`; app-specific linker override drops `__qemu_min_stack_size__ 0x1000 -> 0x0a00`, so `._user_heap_stack 4100 -> 2564` while compiled stack frames stay unchanged and the measured heap peak stays `9456B`; verified in a clean worktree with runtime `223/223` and unit test `649/649` |
| 2026-03-28 | Cut HelloPerformance QEMU reserved stack to 2KB | 2168304 | 52 | 4204 | 4256 | 0 / 9456 | 1200 | `static RAM -512B`; app-specific linker override drops `__qemu_min_stack_size__ 0x0a00 -> 0x0800`, so `._user_heap_stack 2564 -> 2052` while compiled stack frames stay unchanged and the measured heap peak stays `9456B`; verified in a clean worktree with runtime `223/223` and unit test `649/649` |
| 2026-03-28 | Cut HelloPerformance QEMU reserved stack to 1.5KB | 2168304 | 52 | 3692 | 3744 | 0 / 9456 | 1200 | `static RAM -512B`; app-specific linker override drops `__qemu_min_stack_size__ 0x0800 -> 0x0600`, so `._user_heap_stack 2052 -> 1540` while compiled stack frames stay unchanged and the measured heap peak stays `9456B`; verified in a clean worktree with runtime `223/223` and unit test `649/649` |
| 2026-03-29 | Cut HelloPerformance QEMU reserved stack to 1KB | 2168304 | 52 | 3180 | 3232 | 0 / 9456 | 824 | `static RAM -512B`; app-specific linker override drops `__qemu_min_stack_size__ 0x0600 -> 0x0400`, so `._user_heap_stack 1540 -> 1028` while the real fixed `.bss` stays `2152B`. `output/main.map` cross-check shows the former `1200B` / `1112B` / `976B` frames live only in discarded input sections for this app image, leaving `egui_canvas_draw_line_hq (824B)` as the largest linked frame. `python scripts/code_runtime_check.py --app HelloPerformance --keep-screenshots` passed `223/223`, `HelloUnitTest` passed `649/649`, and `python scripts/code_perf_check.py --profile cortex-m3 --threshold 5` PASSED. |
| 2026-03-29 | Add optional QEMU heap action trace for HelloPerformance analysis | 2168304 | 52 | 3180 | 3232 | 0 / 9456 | 824 | `default RAM unchanged`; `QEMU_HEAP_TRACE_ACTIONS=1` now emits per-recording-action `HEAP_ACTION:<idx>:current/peak/allocs/frees` telemetry for heap-measure builds, without affecting normal builds when left at `0`. This is only analysis tooling for chasing the remaining `9456B` heap peak; the shipped low-RAM path, static RAM headline, and current linked stack peak remain unchanged. |
| 2026-03-29 | Shrink HelloPerformance line HQ stack frame and cut QEMU reserved stack to 768B | 2165884 | 52 | 2924 | 2976 | 0 / 9456 | 760 | `text -2420B`, `static RAM -256B`; function-local `optimize("Os")` trims `egui_canvas_draw_line_hq 824 -> 288`, so the largest linked frame drops to `egui_canvas_draw_text_transform (760B)` and the app-specific QEMU reserve can move `__qemu_min_stack_size__ 0x0400 -> 0x0300` (`._user_heap_stack 1028 -> 772`). `output/main.map` still keeps the `1200B` / `1112B` / `976B` frames in discarded input sections only. `python scripts/code_runtime_check.py --app HelloPerformance --keep-screenshots` passed `223/223`, `HelloUnitTest` passed `649/649`, and `python scripts/code_perf_check.py --profile cortex-m3 --threshold 5` PASSED. |
| 2026-03-29 | Shrink HelloPerformance rotated-text draw stack frame | 2160044 | 52 | 2924 | 2976 | 0 / 9456 | 752 | `text -5840B`, static RAM unchanged; function-local `optimize("Os")` trims `egui_canvas_draw_text_transform 760 -> 376`, so the largest linked frame moves again to `egui_canvas_draw_circle_fill_gradient (752B)` while the existing `768B` QEMU reserve still holds. Verified perf stays within the `<=500B => 5%` rule (`TEXT_ROTATE 11.400 -> 11.357`, `TEXT_ROTATE_BUFFERED 11.402 -> 11.360`, `EXTERN_TEXT_ROTATE 12.754 -> 12.711`, `EXTERN_TEXT_ROTATE_BUFFERED 12.756 -> 12.713`), runtime `223/223`, unit test `649/649`, and `python scripts/code_perf_check.py --profile cortex-m3 --threshold 5` PASSED. |
| 2026-03-29 | Stream HelloPerformance external rotated-text glyph scratch in 14-row heap chunks | 2160936 | 52 | 2924 | 2976 | 0 / 9456 | 752 | `text +892B`, static RAM unchanged; external buffered rotated-text drops its visible-tile external glyph scratch from whole-glyph heap blocks to `14`-row chunk streaming, so `EXTERN_TEXT_ROTATE_BUFFERED 4254 -> 4194 (-60B)` while `12.713 -> 12.885 (+1.35%)` stays within the `<=500B => 5%` perf gate. Whole-run heap peak remains `9456B`, PC screenshot stays unchanged, and the active linked stack peak stays `752B`. |
| 2026-03-29 | Cap HelloPerformance external raw-image shared row caches to 2 rows | 2160936 | 52 | 2924 | 2976 | 0 / 9456 | 752 | `static RAM` and whole-run heap stay unchanged, but the default external raw-image row caches now obey the `<= 2 rows/cols` rule: `EGUI_CONFIG_IMAGE_EXTERNAL_DATA_CACHE_MAX_BYTES 1920 -> 960`, `EGUI_CONFIG_IMAGE_EXTERNAL_ALPHA_CACHE_MAX_BYTES 960 -> 480`, and both knobs are now `#ifndef`-wrapped for later A/B. Representative scene-local heap drops `EXTERN_IMAGE_565_8 2880 -> 1440`, `EXTERN_IMAGE_RESIZE_565_8 2976 -> 1536`, `EXTERN_IMAGE_ROTATE_565_8 2880 -> 1440`; runtime screenshots stay `223/223` with `changed=0`. This policy-driven low-RAM default does slow some external raw-image scenes beyond the old `>500B => 10%` heuristic (`EXTERN_IMAGE_565_1 +13.11%`, `EXTERN_IMAGE_ROTATE_565_1 +12.56%`, `EXTERN_IMAGE_ROTATE_TILED_565_8 +12.26%`, while `EXTERN_IMAGE_RESIZE_565_8 +2.16%` and `EXTERN_IMAGE_ROTATE_565_8 +0.41%` stay much lower). |
| 2026-03-29 | Align HelloPerformance external transform row-cache chunk loads | 2161016 | 52 | 2924 | 2976 | 0 / 9456 | 752 | `text +80B`, static RAM/heap/stack unchanged; external image transform now reloads shared row-cache chunks from `rows_per_chunk`-aligned starts instead of the current miss row, which removes reload thrash when rotate sampling bounces within the same `2`-row cache window. Verified gains are concentrated on external rotate scenes: `EXTERN_IMAGE_ROTATE_565_1 14.696 -> 13.474 (-8.32%)`, `EXTERN_IMAGE_ROTATE_565_2 15.508 -> 14.106 (-9.04%)`, `EXTERN_IMAGE_ROTATE_TILED_565_0 11.707 -> 10.630 (-9.20%)`, `EXTERN_IMAGE_ROTATE_TILED_565_8 17.633 -> 16.029 (-9.10%)`; direct draw and resize stay `0.00%`, runtime stays `223/223`, unit test stays `649/649`, and `python scripts/code_perf_check.py --profile cortex-m3` PASSED. |
| 2026-03-29 | Reject HelloPerformance 1-row external raw-image shared row cache | 2161016 | 52 | 2924 | 2976 | 0 / 9456 | 752 | `default unchanged`; `EGUI_CONFIG_IMAGE_EXTERNAL_DATA_CACHE_MAX_BYTES 960 -> 480` and `EGUI_CONFIG_IMAGE_EXTERNAL_ALPHA_CACHE_MAX_BYTES 480 -> 240` cut representative scene-local heap again (`EXTERN_IMAGE_565_8 1440 -> 720`, `EXTERN_IMAGE_RESIZE_565_8 1536 -> 816`, `EXTERN_IMAGE_ROTATE_565_8 1440 -> 720`), but whole-run heap peak still stays `9456B` and key scenes regress past the `>500B => 10%` line (`EXTERN_IMAGE_565_1 +23.11%`, `EXTERN_IMAGE_565_8 +20.67%`, `EXTERN_IMAGE_ROTATE_565_1 +12.52%`, `EXTERN_IMAGE_ROTATE_565_8 +23.81%`), so the shipped default remains the current `2`-row cache. |
| 2026-03-29 | Shrink HelloPerformance remaining linked stack hotspots and cut QEMU reserved stack to 544B | 2140376 | 52 | 2700 | 2752 | 0 / 9456 | 536 | `text -6640B`, `static RAM -224B`; function-local `optimize("Os")` trims the remaining linked stack hotspots enough to move the active peak `752 -> 536`, so the app-specific QEMU reserve can move `__qemu_min_stack_size__ 0x0300 -> 0x0220` (`._user_heap_stack 772 -> 548`). Heap measurement rerun still reports `interaction_total_peak=9456`, `interaction_total_current=0`, `alloc/free=8069/8069`. Verified perf stays green and several geometry/shadow cases get faster: `POLYGON_FILL 2.245 -> 1.572 (-29.98%)`, `GRADIENT_TRIANGLE 2.088 -> 1.783 (-14.61%)`, `GRADIENT_RADIAL 6.679 -> 4.898 (-26.67%)`, `GRADIENT_ANGULAR 6.866 -> 4.900 (-28.63%)`, `GRADIENT_CIRCLE 9.753 -> 6.719 (-31.11%)`, `SHADOW_ROUND 5.536 -> 4.947 (-10.64%)`, while `TEXT_ROTATE_BUFFERED`, `EXTERN_TEXT_ROTATE_BUFFERED`, `MASK_IMAGE_TEST_PERF_ROUND_RECT`, and `EXTERN_MASK_IMAGE_TEST_PERF_ROUND_RECT` stay unchanged. Runtime `223/223`, unit test `649/649`, and `python scripts/code_perf_check.py --profile cortex-m3 --threshold 5` PASSED. |
| 2026-03-29 | Disable default HelloPerformance PFB-height row caches with no measured perf gain | 2139668 | 52 | 2684 | 2736 | 0 / 9360 | 536 | `text -708B`, `static RAM -16B`; default `EGUI_CONFIG_IMAGE_STD_ROUND_RECT_FAST_ROW_CACHE_ENABLE=0` and `EGUI_CONFIG_MASK_CIRCLE_FRAME_ROW_CACHE_ENABLE=0` now compile out the optional `PFB_HEIGHT` round-rect/circle row caches from the shipped low-RAM path. Clean heap-measure QEMU rerun drops `interaction_total_peak 9456 -> 9360` and `alloc/free 8069/8069 -> 5328/5328`, while clean `python scripts/code_perf_check.py --profile cortex-m3 --threshold 5` shows no `>5%` regression and several mask hot scenes get faster: `MASK_IMAGE_QOI_8_ROUND_RECT 3.153 -> 2.781 (-11.80%)`, `EXTERN_MASK_IMAGE_QOI_8_ROUND_RECT 3.629 -> 3.257 (-10.25%)`, `MASK_IMAGE_RLE_8_ROUND_RECT 2.458 -> 2.085 (-15.17%)`, `EXTERN_MASK_IMAGE_RLE_8_ROUND_RECT 3.282 -> 2.909 (-11.37%)`. Runtime screenshot / unit-test suites were not rerun in this round. |
| 2026-03-29 | Trim HelloPerformance polygon/gauge/round-rect gradient stack and cut QEMU reserved stack to 432B | 2132072 | 52 | 2572 | 2624 | 0 / 9360 | 424 | `text -7596B`, `static RAM -112B`; packed polygon edge/intersection scratch plus helper-split gauge draw and function-local `optimize("Os")` on `egui_canvas_draw_round_rectangle_corners_fill_gradient()` move the active linked stack peak `536 -> 424`, so the app-specific QEMU reserve can move `__qemu_min_stack_size__ 0x0220 -> 0x01b0` (`._user_heap_stack 548 -> 436`). Clean heap-measure QEMU rerun keeps `interaction_total_peak=9360`, `interaction_total_current=0`, `alloc/free=5328/5328`. Clean `python scripts/code_perf_check.py --profile cortex-m3 --threshold 5` PASSED, with representative deltas `POLYGON_FILL 1.572 -> 1.587 (+0.95%)`, `GRADIENT_ROUND_RECT 3.717 -> 2.340 (-37.05%)`, and `GRADIENT_ROUND_RECT_CORNERS 2.186 -> 1.280 (-41.45%)`. Runtime screenshot / unit-test suites were not rerun in this round. |
| 2026-03-29 | Move HelloPerformance codec decode states from static RAM to frame heap | 2132620 | 52 | 2356 | 2408 | 0 / 9568 | 424 | `text +548B`, `static RAM -216B`; `qoi_state/rle_state` no longer stay in `.bss`, and only `qoi_state_ptr/rle_state_ptr` remain as `4B + 4B` fixed metadata. The actual decoder states now allocate `208B` (QOI) / `16B` (RLE) only while a frame is decoding and release from `egui_image_std_release_frame_cache()`. Clean whole-run heap rerun changes `interaction_total_peak 9360 -> 9568` and `alloc/free 5328/5328 -> 5372/5372`, while `current` still returns to `0`. Clean `python scripts/code_perf_check.py --profile cortex-m3 --threshold 5` PASSED with representative codec deltas `IMAGE_QOI_565_8 2.258 -> 2.267 (+0.40%)`, `EXTERN_IMAGE_QOI_565_8 2.734 -> 2.744 (+0.37%)`, `IMAGE_RLE_565_8 1.561 -> 1.564 (+0.19%)`, and `EXTERN_IMAGE_RLE_565_8 2.385 -> 2.388 (+0.13%)`. |
| 2026-03-30 | Add default-off core logical PFB probe tooling | 2132928 | 52 | 2356 | 2408 | 0 / 9568 | 424 | `text +308B`, static RAM unchanged; core now exposes a default-off logical PFB probe plus a weak per-scene width hint hook for manual A/B without changing physical `PFB` bytes. Clean heap rerun stays `interaction_total_peak=9568`, `interaction_total_current=0`, `alloc/free=5372/5372`; stack cross-check shows the new helpers are only `32B / 32B / 4B`, so the active linked peak remains `424B`. This is measurement tooling only, and the shipped low-RAM path still keeps the configured `PFB_WIDTH/PFB_HEIGHT` geometry unchanged by default. |
| 2026-03-30 | Direct-blend HelloPerformance QOI visible alpha rows and trim codec heap peak | 2135492 | 52 | 2356 | 2408 | 0 / 9424 | 432 | `text +2564B`, static RAM unchanged; QOI alpha fast paths now decode/blend the first visible span directly into the destination row, and masked QOI visible spans switch to a fixed `48px` stack chunk instead of frame heap scratch. Clean whole-run heap rerun changes `interaction_total_peak 9568 -> 9424` and `alloc/free 5372/5372 -> 4980/4980`, while spot scenes land at `IMAGE_QOI_565_8=9424`, `EXTERN_IMAGE_QOI_565_8=9424`, `MASK_IMAGE_QOI_8_ROUND_RECT=9424`, `EXTERN_MASK_IMAGE_QOI_8_ROUND_RECT=9424`, `IMAGE_RLE_565_8=9376`, `EXTERN_IMAGE_RLE_565_8=9376`. Against clean `758fbdc`, the `<=500B => 5%` gate stays green: `MASK_IMAGE_QOI_8_ROUND_RECT +0.72%`, `EXTERN_MASK_IMAGE_QOI_8_ROUND_RECT +0.58%`, `IMAGE_RLE_565_8 +0.19%`, `EXTERN_IMAGE_RLE_565_8 +0.13%`, while `IMAGE_QOI_565_8` / `EXTERN_IMAGE_QOI_565_8` improve `-6.39% / -5.39%`. Runtime `223/223`, unit test `649/649`, and the current linked stack peak is `egui_canvas_draw_thick_line_scan (432B)`. |
| 2026-03-30 | Trim HelloPerformance rotated-text visible-list heap | 2135512 | 52 | 2356 | 2408 | 0 / 9424 | 432 | `text +20B`, static RAM unchanged; buffered rotated-text keeps the visible-only alpha8 iteration but replaces the visible `layout_glyph*` list with compact layout indices, so `TEXT_ROTATE_BUFFERED 3984 -> 3846 (-138B)` and `EXTERN_TEXT_ROTATE_BUFFERED 4194 -> 4056 (-138B)`. Against clean `6af3f2c`, perf stays flat at `TEXT_ROTATE_BUFFERED 11.360 -> 11.358` and `EXTERN_TEXT_ROTATE_BUFFERED 12.885 -> 12.884`, so the `<=500B => 5%` gate stays green. Whole-run heap remains `9424B`, stack peak remains `432B`, runtime rerun stays `223/223`, unit test stays `649/649`, and `python scripts/code_perf_check.py --profile cortex-m3 --threshold 5` PASSED. |
| 2026-03-30 | Trim HelloPerformance buffered-text alpha8 ceiling to 2560B after heap-cliff audit | 2135512 | 52 | 2356 | 2408 | 0 / 9424 | 432 | `text/static RAM unchanged`; default `EGUI_CONFIG_TEXT_TRANSFORM_VISIBLE_ALPHA8_MAX_BYTES` now trims `3072 -> 2560`, so `TEXT_ROTATE_BUFFERED 3846 -> 3334 (-512B)` and `EXTERN_TEXT_ROTATE_BUFFERED 4056 -> 3544 (-512B)` while whole-run heap stays `9424B` and stack peak stays `432B`. Full perf still passes the stricter `<=500B => 5%` line (`TEXT_ROTATE_BUFFERED 11.358 -> 11.552 (+1.71%)`, `EXTERN_TEXT_ROTATE_BUFFERED 12.884 -> 13.072 (+1.46%)`), runtime stays `223/223`, unit test stays `649/649`, and `python scripts/code_perf_check.py --profile cortex-m3 --threshold 5` PASSED. A fresh heap-cliff audit also shows why `2304B` and below stay rejected on the shipped path: `TEXT_ROTATE_BUFFERED` jumps to `5410B` at `2304B` and `5252B` at `2048/1792/1536/256/0`, because the packed4 fallback scratch becomes larger than the trimmed alpha8 cache. |
| 2026-03-30 | Fix HelloPerformance logical-PFB resize-map probe assert and reject wider probe shapes | 2135512 | 52 | 2356 | 2408 | 0 / 9424 | 432 | `default RAM unchanged`; the heap-backed resize `src_x_map` builders no longer assert on `count <= EGUI_CONFIG_PFB_WIDTH`, so logical-PFB A/B builds can finish when the logical tile width exceeds the physical `48px` buffer width. Clean default reruns stay `interaction_total_peak=9424`, `interaction_total_current=0`, `interaction_delta_allocs/frees=4980/4980`, and the linked stack peak remains `432B`. Verification on the shipped path still passes: `python scripts/code_perf_check.py --profile cortex-m3 --threshold 5`, `HelloUnitTest 649/649`, and `python scripts/code_runtime_check.py --app HelloPerformance --timeout 10 --keep-screenshots`. The new wider logical probe shapes remain rejected: `96x8` fails the whole-scene perf gate badly (`TEXT_ROTATE_BUFFERED +33.13%`, `TEXT +17.51%`, `CIRCLE_FILL +25.29%`, `ANIMATION_TRANSLATE +15.65%`), `128x6` already pushes targeted `IMAGE_RLE_565` to `+11.07%`, and `192x4` degrades `IMAGE_RLE_565` to `+25.52%` plus `MASK_IMAGE_QOI_8_ROUND_RECT +17.83%`. |
| 2026-03-30 | Move HelloPerformance masked QOI visible scratch back to heap | 2135500 | 52 | 2356 | 2408 | 0 / 9568 | 432 | `text -12B`, static RAM unchanged; masked QOI visible-span scratch now follows the current clipped width on transient heap instead of fixed `48px` stack arrays, which restores the project rule for size-related buffers. Clean whole-run heap rerun changes `interaction_total_peak 9424 -> 9568` and `alloc/free 4980/4980 -> 5132/5132`, while unmasked QOI scenes stay `IMAGE_QOI_565_8=9424` / `EXTERN_IMAGE_QOI_565_8=9424` and masked QOI round-rect scenes rise to `MASK_IMAGE_QOI_8_ROUND_RECT=9568` / `EXTERN_MASK_IMAGE_QOI_8_ROUND_RECT=9568`. The active linked stack peak stays `432B`, and `egui_image_qoi_decode_row_rgb565_alpha8_masked_split 216 -> 56`. Verification still passes: `python scripts/code_perf_check.py --profile cortex-m3 --threshold 5`, `HelloUnitTest 649/649`, and `python scripts/code_runtime_check.py --app HelloPerformance --timeout 10 --keep-screenshots`. |
| 2026-03-30 | Reuse HelloPerformance tail-row heap for masked QOI visible scratch | 2135580 | 52 | 2356 | 2408 | 0 / 9424 | 432 | `text +80B`, static RAM unchanged; masked QOI visible-span scratch still obeys the size-related-buffer rule, but now borrows the already-allocated tail-row heap row when the current clipped span fits. On the shipped `48px` walk this removes the extra transient allocation, so clean whole-run heap rerun changes `interaction_total_peak 9568 -> 9424` and `alloc/free 5132/5132 -> 4980/4980`, while masked QOI round-rect scenes return to `MASK_IMAGE_QOI_8_ROUND_RECT=9424` / `EXTERN_MASK_IMAGE_QOI_8_ROUND_RECT=9424` with `3 / 3` alloc/free. Perf also improves slightly (`MASK_IMAGE_QOI_8_ROUND_RECT 2.812 -> 2.806`, `EXTERN_MASK_IMAGE_QOI_8_ROUND_RECT 3.289 -> 3.282`), and runtime `223/223`, unit test `649/649`, plus `python scripts/code_perf_check.py --profile cortex-m3 --threshold 5` all PASSED. |
| 2026-03-30 | Accept scene-selective logical `96x8` walk for HelloPerformance codec alpha scenes | 2135720 | 52 | 2356 | 2408 | 0 / 6688 | 432 | `text +140B`, static RAM unchanged; `egui_core_get_logical_pfb_target_width_hint()` now returns `96` only for codec alpha scenes and keeps `0` for non-codec scenes, so the physical `48x16` PFB bytes stay unchanged while those scenes reuse a logical `96x8` walk. Clean whole-run heap rerun changes `interaction_total_peak 9424 -> 6688` and `alloc/free 4980/4980 -> 5308/5308`; clean full-benchmark compare shows no regression `>10%`, with the worst case `IMAGE_TILED_RLE_565_8 +4.54%`. Current single-scene heap spot checks land at `MASK_IMAGE_QOI_ROUND_RECT=6688`, `EXTERN_MASK_IMAGE_QOI_ROUND_RECT=6688`, `MASK_IMAGE_QOI_CIRCLE=6688`, `EXTERN_MASK_IMAGE_QOI_CIRCLE=6688`, `IMAGE_TILED_QOI_565_8=5008`, `IMAGE_TILED_RLE_565_8=4816`, `IMAGE_RLE_565_8=3760`, `EXTERN_IMAGE_RLE_565_8=3760`, `IMAGE_QOI_565_8=3664`, `EXTERN_IMAGE_QOI_565_8=3664`, `MASK_IMAGE_QOI_8_ROUND_RECT=3664`, and `EXTERN_MASK_IMAGE_QOI_8_ROUND_RECT=3664`. Runtime `223/223`, unit test `649/649`, the active linked stack peak stays `432B`, and the new app-side helper frames are only `16B` / `8B`. |
| 2026-03-30 | Lazy-allocate HelloPerformance opaque masked codec alpha rows | 2135708 | 52 | 2356 | 2408 | 0 / 6448 | 432 | `text -12B`, static RAM unchanged; opaque masked codec draws no longer reserve a full `240B` row-cache alpha backing store up front. The fallback opaque-alpha row now grows lazily to the current clipped span only when blending actually needs it, so clean whole-run heap rerun changes `interaction_total_peak 6688 -> 6448` and `alloc/free 5308/5308 -> 5286/5286`. The stricter `<=500B => 5%` gate stays green (`python scripts/code_perf_check.py --profile cortex-m3 --threshold 5` PASSED), runtime stays `223/223`, unit test stays `649/649`, and a single-scene heap rerun confirms `MASK_IMAGE_QOI_ROUND_RECT=6448` with `17 / 17` alloc/free. The same masked-QOI round-rect/circle hotspot group remains the current big head by construction because only its eager opaque-alpha reserve changed. |
| 2026-03-30 | Extend HelloPerformance logical `96x8` walk to codec opaque hotspots | 2135636 | 52 | 2356 | 2408 | 0 / 5008 | 432 | `text -72B`, static RAM unchanged; the per-scene logical-width hint now keeps `96x8` for the previous codec-alpha set and additionally covers the current high-heap codec opaque hotspots. `IMAGE_TILED_RLE_565_0` is explicitly left on the default walk because the wider hint regresses it by `+13.10%`. Clean whole-run heap rerun changes `interaction_total_peak 6448 -> 5008` and `alloc/free 5286/5286 -> 5434/5434`; the first peak owner is `IMAGE_TILED_QOI_565_8`. Heap-measure build size is `text/data/bss = 2136724/56/2360`. Clean full-benchmark compare vs `cfd723e` shows no regression `>10%`; worst cases are `MASK_IMAGE_RLE_NO_MASK +8.80%`, `IMAGE_RLE_565 +8.74%`, `IMAGE_TILED_QOI_565_0 +8.44%`, and `MASK_IMAGE_RLE_IMAGE +8.10%`. Runtime `223/223`, unit test `649/649`, the active linked stack peak stays `432B`, and the renamed helper frames are still only `16B / 8B`. |

## Current Breakdown

### Top Static RAM Symbols

| Symbol | Section | Size (B) | Notes |
| --- | --- | ---: | --- |
| `egui_pfb` | `.bss` | 1536 | User-configurable PFB, not an optimization target |
| `egui_core` | `.bss` | 120 | Core runtime state; removed cached `pfb_total_buffer_size`, `touch=0` strips dead root-group touch flags, and HelloPerformance aliases the user root to the main root group |
| `g_text_transform_prepare_cache` | `.bss` | 60 | Tiny affine prepare cache restored for rotated-text scenes; fixed metadata only, not size-related scratch |
| `test_view` | `.bss` | 56 | HelloPerformance scene object |
| `canvas_data` | `.bss` | 32 | Canvas runtime state; HelloPerformance compiles out extra clip/spec-circle metadata |
| `ui_timer` | `.bss` | 20 | App timer state |
| `port_display_driver` | `.data` | 16 | QEMU display driver state |
| `g_font_std_code_lookup_cache` | `.data` | 12 | Font lookup cache |
| `egui_image_decode_cache_state` | `.data` | 12 | Decode cache metadata; now also records tail-row cache column window for the low-RAM codec mode |
| `g_text_transform_visible_tile_cache` | `.bss` | 8 | Visible alpha8 tile heap cache handle |
| `qoi_state_ptr` | `.bss` | 4 | Heap handle for the transient QOI decoder state |
| `rle_state_ptr` | `.bss` | 4 | Heap handle for the transient RLE decoder state |
| `s_qemu_resource_handle` | `.data` | 4 | External resource semihosting handle |

- This build no longer contains `egui_input_info`, `input_motion_pool*`, or `egui_view_group_touch_state`.
- Current normal QEMU build: `text=2135132`, `data=72`, `bss=2600`, `static RAM=2672`.
- Section split from `llvm-size -A`: `.data=72`, `.bss=2164`, `._user_heap_stack=436`.
- `HelloPerformance` now overrides the QEMU linker reserve with `__qemu_min_stack_size__=0x01b0`, so the reported static RAM headline reflects a 432B stack budget instead of the port default 8KB.
- A 2026-03-30 follow-up A/B tried to push the reserve down to `0x0180` after temporarily splitting `egui_canvas_draw_thick_line_scan()` to `384B`, but that probe was rejected and not kept. Fresh clean `-fstack-usage` plus `main.map`/`llvm-nm` cross-checks showed the real linked ceiling would then move to `egui_canvas_draw_circle_fill_gradient (424B)`, followed by `egui_canvas_draw_rectangle_fill_gradient (416B)`, `egui_canvas_draw_polygon_fill_gradient (408B)`, `egui_canvas_draw_polygon_fill (408B)`, and `egui_image_rle_draw_image (392B)`, so lowering the shipped reserve below `0x01b0` would only recover `<= 24B` before hitting the next verified hotspot.
- Fixed resident `.data + .bss` is now `2236B`; `egui_pfb=1536B` still dominates that total and remains user-configurable rather than a core optimization target.
- Core still keeps the default-off global logical-PFB probe tooling for manual A/B, but HelloPerformance now uses the weak width-hint hook in the shipped path for the current high-heap codec scenes: `egui_core_get_logical_pfb_target_width_hint()` returns `96` for the selected QOI/RLE opaque+alpha hotspots and `0` elsewhere. Non-hotspot scenes therefore keep the configured logical/physical `48x16` walk, and `IMAGE_TILED_RLE_565_0` stays on the default walk because the wider hint exceeds the current perf gate.
- The app-side logical-width hint remains tiny on stack: `egui_view_test_performance_uses_logical_pfb_96_hint()` is `16B`, `egui_core_get_logical_pfb_target_width_hint()` in `uicode.c` is `8B`, and the active linked stack peak still stays at `432B`.
- The same probe infrastructure now also leaves the heap-backed resize `src_x_map` path runtime-safe when the logical tile width is wider than the physical `48px` PFB tile. This only affects manual probe builds; the shipped default still uses the configured `48x16` geometry and keeps the same RAM headline.
- The latest rotated-text perf restore adds only fixed metadata: `g_text_transform_prepare_cache (60B)` plus `s_dim_font/s_dim_string/s_dim_w/s_dim_h (12B)`; both are tiny caches and do not scale with font size, image size, screen size, or `PFB` size.
- `g_text_transform_visible_tile_cache` still only stores the `8B` heap handle/capacity pair; the visible alpha8 tile itself remains heap-backed, and HelloPerformance now keeps the reuse ceiling at `2560B`. The latest visible-list compaction reopened this step under the stricter `<=500B => 5%` gate, while the fresh heap-cliff audit shows that `2304B` and below are not acceptable defaults because the packed4 fallback pushes `TEXT_ROTATE_BUFFERED` up to `5410B / 5252B` scene-local heap instead of reducing it.
- The default HelloPerformance build no longer carries any fixed `PFB_HEIGHT` row-cache metadata for circle masks; `EGUI_CONFIG_MASK_CIRCLE_FRAME_ROW_CACHE_ENABLE` now defaults to `0`, and re-enabling it still routes the size-related scratch through heap rather than static RAM.
- The codec row-band pixel/alpha cache no longer occupies fixed `.bss`; it is now allocated per frame from heap because its required size follows active image width, alpha format, and `PFB` row-band height.
- The single-row decode pixel/opaque-alpha scratch also no longer occupies fixed `.bss`; it is now allocated per frame from heap because `EGUI_CONFIG_IMAGE_DECODE_ROW_BUF_WIDTH` follows screen width in the default config, and the required bytes follow pixel format / alpha row width.
- QOI/RLE decoder state itself also no longer stays in fixed SRAM: `qoi_state_ptr` / `rle_state_ptr` are only `4B` static handles, while the actual decoder state is now `208B` (QOI) / `16B` (RLE) on frame heap and is released from `egui_image_std_release_frame_cache()`.
- The current default whole-run heap peak is now `5008B`, and the first peak owner is `IMAGE_TILED_QOI_565_8`. Extending the same logical `96x8` walk to the current opaque codec hotspots removes the old `6448B` masked-QOI headline; the current confirmed tiers are `IMAGE_TILED_QOI_565_8 5008B`, `IMAGE_TILED_RLE_565_8 4816B`, QOI/RLE alpha hotspots `3760B / 3664B`, buffered text `3544B / 3334B`, and external raw-image `1536B / 1440B`.
- There is no longer any decode row scratch parked in fixed SRAM between frames; the remaining static decode footprint is only metadata such as `qoi_state_ptr`, `rle_state_ptr`, and `egui_image_decode_cache_state`.
- The animation benchmark helper no longer parks `anim_perf_view`, `anim_perf_interp`, or an init flag in fixed SRAM; each animation scene now uses a tiny local context and releases it with the draw call.
- `EGUI_CONFIG_IMAGE_CODEC_TAIL_ROW_CACHE_ENABLE=1` is the current HelloPerformance low-RAM codec mode: only the later-tile tail columns remain in the row-band cache, while the first visible span stays transient and follows the active codec/path.
- The current default codec path no longer allocates a full-width first-tile scratch row: QOI alpha rows now direct-blend the first visible span, masked QOI rows keep their size-related scratch heap-backed but borrow the existing tail-row heap row when `cache_col_count >= count` (the shipped `48px` walk), and only fall back to a separate transient heap block when a wider clipped span does not fit. RLE alpha rows still keep visible-width heap scratch before the later-tile tail window is cached.
- The decode row-band metadata no longer stores an unused `row_count`; the current `12B` tracks `image_info`, `row_band_start`, `cache_col_start`, `cache_col_count`, and cache mode, which is the only fixed-RAM cost of the tail-row cache policy.
- The codec single-row pixel scratch no longer keeps a second persistent heap handle/capacity pair when row-band cache is enabled; it now borrows the same row-cache pixel buffer and invalidates any previous cache tag before reuse.
- Under the current HelloPerformance decode bounds (`single-row <= 480B`, `row-cache pixel <= 7680B`, `row-cache alpha <= 3840B`), the remaining decode capacity metadata also compacts from 32-bit to 16-bit counters.
- HelloPerformance now opts into an 8-bit QOI current-row cursor because its generated QOI resources are only `40/120/240` pixels tall; `egui_image_qoi_prepare_decode_info()` asserts that bound before decoding.
- `egui_core` no longer stores a cached `pfb_total_buffer_size`; the few resize/clear paths derive bytes or pixel count from `pfb_width * pfb_height` on demand, which keeps the perf check within noise while removing fixed core RAM.
- With `EGUI_CONFIG_FUNCTION_SUPPORT_TOUCH=0`, `egui_view_root_group_t` no longer carries the `is_disallow_process_touch_event` field, so the root-group object shrinks from `64B` to `60B`.
- With `EGUI_CONFIG_CORE_SEPARATE_USER_ROOT_GROUP_ENABLE=0`, HelloPerformance no longer keeps a second `user_root_view_group`; `egui_core_get_user_root_view()` and the add/remove/layout helpers now alias directly to `root_view_group`, which cuts another `60B` from `egui_core` without affecting the single-root benchmark app flow.
- With `EGUI_CONFIG_CANVAS_EXTRA_CLIP_ENABLE=0` and `EGUI_CONFIG_CANVAS_SPEC_CIRCLE_INFO_ENABLE=0`, `canvas_data` no longer carries the scroll/list extra-clip pointer or out-of-range circle registration metadata; HelloPerformance stays within built-in circle LUT range and does not use nested clip widgets.
- The QEMU port now uses a self-managed runtime heap carved from `[_ebss, _estack - _Min_Stack_Size)`, so enabling `EGUI_CONFIG_QEMU_PLATFORM_MALLOC_ENABLE` does not pull in newlib `malloc` state.
- The default QEMU build no longer keeps heap measurement counters in fixed SRAM; only the allocator topology (`free_list` / init flag) remains resident, while `QEMU_HEAP_MEASURE=1` recompiles the counters back in for telemetry.
- With `EGUI_CONFIG_TEXT_TRANSFORM_LAYOUT_HEAP_ENABLE=0`, HelloPerformance no longer keeps persistent text-layout fallback pointers/capacities in fixed SRAM; with `EGUI_CONFIG_TEXT_TRANSFORM_SCRATCH_HEAP_ENABLE=1`, the active rotated-text layout/tile scratch now follows actual glyph and line counts on transient heap instead of bounded hot-path stack arrays.
- HelloPerformance no longer keeps macro-sized external font row/glyph/pixel scratch on stack; direct external glyph draw still allocates a small scanline cache by actual glyph dimensions on heap, while the visible-tile external rotated-text path now streams glyph data through a `14`-row heap chunk scratch instead of keeping a whole-glyph heap block resident for the tile.
- HelloPerformance no longer keeps `PFB_WIDTH`-sized resize maps or `PFB_HEIGHT`-sized round-rect row caches on stack; image resize scratch still allocates from heap by current draw width, while the optional round-rect row cache is now disabled by default and only allocates from heap when explicitly re-enabled for measurement.
- HelloPerformance no longer keeps `PFB_HEIGHT`-sized circle-mask row caches inside mask objects; the optional circle-mask frame row cache is also disabled by default now, and when re-enabled it still uses heap because the required bytes follow tile height.
- There is still no size-related heap that stays resident at idle for rotated text, external text, image resize, or circle mask; the new text/font/image/circle scratch buffers are allocated only inside the active draw or frame walk and return to `0B` current heap after the recording finishes.

### Small-Macro Cleanup Follow-Up

- A clean 2026-03-31 rebuild at `96dae0b` confirms the current shipped small-macro-cleanup baseline at `static RAM=2672B` (`.data=72`, `.bss=2164`, `._user_heap_stack=436`).
- `EGUI_CONFIG_FUNCTION_SUPPORT_TOUCH=1` was rechecked against the same `96dae0b` HelloPerformance QEMU perf baseline and is still rejected for the current rule: static RAM rises `2672 -> 3000 (+328B)`, while the worst benchmark regression is `ANIMATION_SCALE 0.320 -> 0.373 ms (+16.56%)`.
- Under the current `<100B static RAM` and `<5%` perf rule, there is still no remaining HelloPerformance app-side override that qualifies for removal: `EGUI_CONFIG_CORE_SEPARATE_USER_ROOT_GROUP_ENABLE=1` still regresses about `+19.92%`, `EGUI_CONFIG_IMAGE_STD_ALPHA_OPAQUE_CACHE_SLOTS=4` still regresses about `+10% ~ +18%`, and `EGUI_CONFIG_IMAGE_RLE_EXTERNAL_CACHE_WINDOW_SIZE 64 -> 1024` costs `+1016B` static RAM.

| Macro | Current value | Why it stays | Current status |
| --- | ---: | --- | --- |
| `EGUI_CONFIG_FUNCTION_SUPPORT_TOUCH` | `0` | Static-RAM item, but reverting to the default touches active animation/input code paths | Reject removal: `+328B`, worst perf regression `+16.56%` |
| `EGUI_CONFIG_CORE_SEPARATE_USER_ROOT_GROUP_ENABLE` | `0` | Static-RAM item, but default `1` reintroduces the extra user-root wrapper and forces the activity/dialog dependency chain back on | Reject removal: about `+56B`, worst perf regression about `+19.92%` |
| `EGUI_CONFIG_FUNCTION_SUPPORT_ACTIVITY` / `EGUI_CONFIG_FUNCTION_SUPPORT_DIALOG` | `0 / 0` | Not independently removable in the current config: `dialog=1` implies `activity=1`, and `activity=1` requires `user_root=1` | Blocked by the rejected `user_root=1` path above |
| `EGUI_CONFIG_IMAGE_STD_ALPHA_OPAQUE_CACHE_SLOTS` | `0` | Static-RAM item, but restoring the default cache hurts active image paths too much | Reject removal: worst perf regression about `+10% ~ +18%` |
| `EGUI_CONFIG_IMAGE_RLE_EXTERNAL_CACHE_WINDOW_SIZE` | `64` | Static-RAM item, but the default window is far above the small-macro threshold | Keep override: `64 -> 1024` costs `+1016B` static RAM |
| `EGUI_CONFIG_IMAGE_DECODE_MAX_PIXEL_SIZE` / `EGUI_CONFIG_IMAGE_DECODE_OPAQUE_ALPHA_ROW_USE_ROW_CACHE` / `EGUI_CONFIG_IMAGE_QOI_CHECKPOINT_COUNT` / `EGUI_CONFIG_IMAGE_CODEC_TAIL_ROW_CACHE_ENABLE` | app override | These are heap/decode-path controls rather than `<100B` static-RAM knobs | Out of scope for this small static-RAM cleanup round |
| `EGUI_CONFIG_SHADOW_DSQ_LUT_MAX` / `EGUI_CONFIG_TEXT_TRANSFORM_*` | app override | These govern stack or transient heap ceilings, not fixed static RAM | Out of scope for this small static-RAM cleanup round |
| Codec/image-format/external-resource feature macros | app override | These are benchmark capability switches, not RAM A/B cleanup knobs | Keep as app feature selection, not part of this cleanup rule |

### Current Override Inventory

- Benchmark geometry/runtime overrides still owned by `example/HelloPerformance/app_egui_config.h`: `EGUI_CONFIG_SCEEN_HEIGHT=240`, `EGUI_CONFIG_MAX_FPS=1`, `EGUI_CONFIG_PFB_WIDTH=48`, `EGUI_CONFIG_PFB_HEIGHT=16`, `EGUI_CONFIG_PFB_BUFFER_COUNT=1`, `EGUI_CONFIG_CIRCLE_SUPPORT_RADIUS_BASIC_RANGE=241`, and `EGUI_CONFIG_DEBUG_LOG_LEVEL=EGUI_LOG_IMPL_LEVEL_INF`. `EGUI_CONFIG_SCEEN_WIDTH` now inherits the library default `240`. These define the benchmark environment itself rather than a small static-RAM toggle.
- Benchmark capability-selection overrides still stay app-local: `EGUI_CONFIG_FUNCTION_SUPPORT_SHADOW=1`, `EGUI_CONFIG_FUNCTION_SUPPORT_MASK=1`, `EGUI_CONFIG_FUNCTION_IMAGE_FORMAT_RGB565_1/2/8=1`, `EGUI_CONFIG_FUNCTION_IMAGE_FORMAT_ALPHA_8=1`, `EGUI_CONFIG_FUNCTION_EXTERNAL_RESOURCE=1`, `EGUI_CONFIG_IMAGE_CODEC_QOI_ENABLE=1`, and `EGUI_CONFIG_IMAGE_CODEC_RLE_ENABLE=1`. These are required to keep the current HelloPerformance scene set compiled in.
- Remaining static-RAM-sensitive overrides are now fully classified: `EGUI_CONFIG_FUNCTION_SUPPORT_TOUCH=0`, `EGUI_CONFIG_CORE_SEPARATE_USER_ROOT_GROUP_ENABLE=0`, `EGUI_CONFIG_FUNCTION_SUPPORT_ACTIVITY=0`, `EGUI_CONFIG_FUNCTION_SUPPORT_DIALOG=0`, `EGUI_CONFIG_IMAGE_STD_ALPHA_OPAQUE_CACHE_SLOTS=0`, and `EGUI_CONFIG_IMAGE_RLE_EXTERNAL_CACHE_WINDOW_SIZE=64`. Each item is now either explicitly rejected by perf, blocked by the rejected `user_root=1` dependency chain, or over the `<100B` SRAM line.
- Remaining decode-path heap controls stay app-local by design: `EGUI_CONFIG_IMAGE_CODEC_ROW_CACHE_ENABLE=1`, `EGUI_CONFIG_IMAGE_DECODE_MAX_PIXEL_SIZE=2`, `EGUI_CONFIG_IMAGE_DECODE_OPAQUE_ALPHA_ROW_USE_ROW_CACHE=1`, `EGUI_CONFIG_IMAGE_QOI_CHECKPOINT_COUNT=0`, and `EGUI_CONFIG_IMAGE_CODEC_TAIL_ROW_CACHE_ENABLE=1`. These tune transient decode heap shape and active codec behavior, not fixed static RAM.
- Remaining stack/heap ceiling overrides also stay app-local by design: `EGUI_CONFIG_SHADOW_DSQ_LUT_MAX=64`, `EGUI_CONFIG_TEXT_TRANSFORM_LAYOUT_PIXEL_INDEX_16BIT=1`, `EGUI_CONFIG_TEXT_TRANSFORM_LAYOUT_LINE_INDEX_16BIT=1`, `EGUI_CONFIG_TEXT_TRANSFORM_SCRATCH_HEAP_ENABLE=1`, and `EGUI_CONFIG_TEXT_TRANSFORM_VISIBLE_ALPHA8_MAX_BYTES=2560`. These are current stack/transient-heap policy points, so they are tracked here but are outside this small static-RAM cleanup rule.
- There is no remaining fixed-on test-side image toggle in `example/HelloPerformance/egui_view_test_performance.c`. The app-side `IMAGE_LARGE` / `IMAGE_SMALL` / `IMAGE_DOUBLE` overrides have already been removed, and their fixed-on compile-time gates have been folded away as well.

### Heap Measurement

| Build | idle current | idle peak | interaction delta current | interaction delta peak | interaction total current | interaction total peak | action count |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| `QEMU_HEAP_MEASURE=1` | 0 | 0 | 0 | 5008 | 0 | 5008 | 222 |

- Heap log contains `HEAP_EXIT`.
- Add `QEMU_HEAP_TRACE_ACTIONS=1` to the measurement build when action-level attribution is needed; QEMU will then also print `HEAP_ACTION:<index>:current=<...>:peak=<...>:allocs=<...>:frees=<...>` after each recorded action, while the default `0` keeps normal RAM/perf builds unchanged.
- Interaction alloc/free count is now `5434 / 5434`; the current default still returns to `current heap=0` after the recording finishes.
- The current default low-RAM path now reports a `5008B` whole-run heap peak, with the first peak owner at `IMAGE_TILED_QOI_565_8`. This accepted shipped path is still not the rejected full-scene logical-PFB probe: only the selected high-heap codec scenes ask for a logical width hint of `96`, so the physical `48x16` PFB bytes stay unchanged and non-hotspot scenes still walk at `48x16`. The latest follow-up extends that shipped hint from the earlier codec-alpha subset to the current opaque codec hotspots as well, while explicitly leaving `IMAGE_TILED_RLE_565_0` on the default walk because it regresses `+13.10%`.
- The buffered rotated-text alpha8 fast-path ceiling now stays at `2560B`: the latest visible-list compaction re-opened this step, so the current shipped default trims `3072 -> 2560` and drops both buffered text hotspots another `512B` (`TEXT_ROTATE_BUFFERED 3846 -> 3334`, `EXTERN_TEXT_ROTATE_BUFFERED 4056 -> 3544`) while still keeping full perf inside the stricter `<=500B => 5%` line (`11.358 -> 11.552`, `12.884 -> 13.072`).
- A fresh heap-cliff audit shows why lower ceilings are still rejected even though the timing delta looks small: once the ceiling drops to `2304B`, the path falls back to packed4 scratch and `TEXT_ROTATE_BUFFERED` jumps to `5410B`; `2048B` and below stay at `5252B`, including `256B` and `0B`. So the current constraint is heap shape, not timing.
- The new rotated-text scratch is owned by `egui_canvas_draw_text_transform()`, allocated as two transient blocks after measuring the current string layout, and released at `cleanup`; it does not stay resident at idle.
- Relative to the pre-rotated-text-heap baseline (`5683 / 5683`), the current low-RAM path still uses transient heap heavily, but the whole-run recording now sits at `5434 / 5434` alloc/free and the buffered text spot scenes themselves stay bounded at `3334B` (`151 / 151`) and `3544B` (`214 / 214`). The active text scenes still satisfy the current RAM/perf gate (`<=500B => 5%`, `>500B => 10%`) and all size-related buffers return to `0B` current heap at idle.
- The new image-size / `PFB`-size scratch does not stay resident after rendering; resize `src_x_map` is still allocated per draw call and released immediately after use. The optional round-rect fast row cache remains heap-backed when explicitly re-enabled, and must stay documented if anyone wants to make it persistent again.
- The optional circle-mask frame scratch is also still required to use heap if re-enabled later, because it scales with `PFB_HEIGHT`; the current default keeps it compiled out rather than spending `96B` for a measured perf-neutral path.
- The low-RAM codec mode is owned by `egui_image_qoi_draw_image()` / `egui_image_rle_draw_image()`: unmasked QOI alpha fast paths still decode/blend the first visible span directly into the destination row, and masked QOI rows now keep the same size-related scratch heap-backed while borrowing the existing tail-row heap row when the current clipped span fits. Only if `cache_col_count < count` do they fall back to a separate transient heap allocation. RLE currently reuses the direct first-visible-tile path only for fast-copy rows, so its alpha scenes still keep the `144B` visible scratch on heap.
- The frame-local codec state is also owned by the same QOI/RLE draw path: QOI adds `208B` and RLE adds `16B`, both allocate on first use in the current frame, and `egui_image_std_release_frame_cache()` releases them before idle so `current heap` still returns to `0B`.
- The latest extension of the same shipped logical `96x8` hint follows the current RAM/perf rule because it saves another `1440B` whole-run heap on top of `cfd723e` (`6448 -> 5008`) while the clean full-benchmark compare still shows no regression beyond `10%`; the worst measured cases are `MASK_IMAGE_RLE_NO_MASK +8.80%`, `IMAGE_RLE_565 +8.74%`, `IMAGE_TILED_QOI_565_0 +8.44%`, and `MASK_IMAGE_RLE_IMAGE +8.10%`.
- The measured heap build reaches the former `.bss` cache footprint only while decode-heavy or rotated-text scenes are active, and releases it back to `0B` at idle.
- External image draw/transform now stays on the existing row-cache path only. Whole-image resident cache is no longer accepted on HelloPerformance, because it violates the project rule against image-sized resident heap.
- The default external raw-image shared row caches now also honor the same ceiling: `EGUI_CONFIG_IMAGE_EXTERNAL_DATA_CACHE_MAX_BYTES=960` and `EGUI_CONFIG_IMAGE_EXTERNAL_ALPHA_CACHE_MAX_BYTES=480`, which is exactly `2` rows for the `240px` RGB565+alpha scenes. This drops representative external raw-image scene-local heap from `2880/2976/2880` to `1440/1536/1440` for `EXTERN_IMAGE_565_8`, `EXTERN_IMAGE_RESIZE_565_8`, and `EXTERN_IMAGE_ROTATE_565_8`, while the current whole-run recording headline still stays pinned by codec scenes at `5008B`.
- The latest external transform round keeps those same `2`-row caches and therefore does not move the `1440B` / `1536B` scene-local heap numbers. Its gain comes from reloading each miss from the aligned chunk start, which cuts source-row backtrack churn inside rotate paths: `EXTERN_IMAGE_ROTATE_565_1 -8.32%`, `EXTERN_IMAGE_ROTATE_565_2 -9.04%`, `EXTERN_IMAGE_ROTATE_TILED_565_0 -9.20%`, `EXTERN_IMAGE_ROTATE_TILED_565_8 -9.10%`, while direct draw and resize remain unchanged.

### Row-Band Cache A/B

Historical A/B data from the full-width row-cache study baseline on 2026-03-28, before the later alpha8 text tile-scratch timing cleanup and before the current tail-row-cache low-RAM mode. The conclusion still stands: row-cache must stay enabled by default.

| Config | text | data | bss | static RAM | interaction total peak | Key result |
| --- | ---: | ---: | ---: | ---: | ---: | --- |
| Historical default (`row-cache on`, full-width cache) | 2160724 | 48 | 10344 | 10392 | 11616 | Baseline before the later tail-row-cache low-RAM mode |
| Experiment (`row-cache off`) | 2154296 | 44 | 13236 | 13280 | 7520 | Heap peak `-4096B`, but static RAM `+2888B` and QOI/RLE scenes collapse |

- The default HelloPerformance config now keeps the row-cache related macros overridable via `USER_CFLAGS` only for measurement; default behavior and default RAM numbers remain unchanged.
- The current default keeps row-cache enabled and additionally turns on `EGUI_CONFIG_IMAGE_CODEC_TAIL_ROW_CACHE_ENABLE=1`; together with the later first-tile scratch split, the default removal of the optional round-rect/circle row caches, the frame-heap decoder-state move, the scene-selective logical `96x8` high-heap codec walk, and the latest opaque-hotspot follow-up, this leaves the shipped whole-run heap headline at `5008B` without reverting to the catastrophic `row-cache off` behavior.
- Turning row-band cache off saves `4096B` peak heap in the measurement build, but it is not an acceptable tradeoff because the compressed-image fast path depends on this cache.
- Measured compressed-image regressions with `row-cache off` include `IMAGE_TILED_QOI_565_0 1.232 -> 46.212 (+3651%)`, `IMAGE_QOI_565 12.271 -> 434.547 (+3441%)`, `EXTERN_IMAGE_QOI_565 18.747 -> 589.348 (+3044%)`, and `EXTERN_IMAGE_RLE_565 4.647 -> 91.420 (+1867%)`.
- Across `44` QOI/RLE related benchmark cases, the average slowdown is `+1314.6%`, so this cache stays enabled by default even under the current heap-pressure goal.

### Scene-Local Heap Hotspots

Single-scene heap spot checks use `QEMU_HEAP_MEASURE=1`, `QEMU_HEAP_ACTIONS_APP_RECORDING=1`, `EGUI_CONFIG_RECORDING_TEST=1`, and `EGUI_TEST_CONFIG_SINGLE_TEST=<scene>`.

| Scene | interaction total peak | alloc/free | Current big head |
| --- | ---: | ---: | --- |
| `IMAGE_TILED_QOI_565_8` | 5008 | `3 / 3` | Current first whole-run peak owner after extending the shipped logical `96x8` walk to the high-heap codec opaque hotspots |
| `IMAGE_TILED_RLE_565_8` | 4816 | `3 / 3` | Tiled RLE alpha path after the same logical `96x8` walk |
| `IMAGE_RLE_565_8` | 3760 | `63 / 63` | RLE alpha scene after the scene-selective logical `96x8` walk |
| `EXTERN_IMAGE_RLE_565_8` | 3760 | `63 / 63` | External RLE alpha scene |
| `IMAGE_QOI_565_8` | 3664 | `3 / 3` | QOI alpha scene after the scene-selective logical `96x8` walk |
| `EXTERN_IMAGE_QOI_565_8` | 3664 | `3 / 3` | External QOI alpha scene |
| `MASK_IMAGE_QOI_8_ROUND_RECT` | 3664 | `3 / 3` | Masked QOI alpha8 round-rect scene; now well below the current tiled-codec top tier |
| `EXTERN_MASK_IMAGE_QOI_8_ROUND_RECT` | 3664 | `3 / 3` | External masked QOI alpha8 round-rect scene |
| `EXTERN_TEXT_ROTATE_BUFFERED` | 3544 | `214 / 214` | External rotated-text visible alpha8 tile cache with the new `2560B` ceiling, compact visible-layout index list, `14`-row external glyph chunk scratch, and transient layout/tile scratch |
| `TEXT_ROTATE_BUFFERED` | 3334 | `151 / 151` | Rotated-text visible alpha8 tile cache with the new `2560B` ceiling, compact visible-layout index list, and transient layout/tile scratch |
| `EXTERN_IMAGE_RESIZE_565_8` | 1536 | `77 / 77` | `2`-row external shared RGB565+alpha row cache plus resize `src_x_map` scratch |
| `EXTERN_IMAGE_565_8` | 1440 | `2 / 2` | `2`-row external shared RGB565+alpha row cache |
| `EXTERN_IMAGE_ROTATE_565_8` | 1440 | `2 / 2` | `2`-row external transform-side shared RGB565+alpha row cache |

- The current whole-run heap peak is still set by compressed-image scenes, not by text scenes, and the first whole-run peak owner is now `IMAGE_TILED_QOI_565_8` at `5008B`, while the buffered text hotspots stay at `3544B / 3334B`.
- The current shipped logical `96x8` path now splits compressed-image heap into three practical tiers: the current top tiled codec hotspots at `5008B / 4816B`, the previous `_8` alpha scenes at `3760B / 3664B`, and the buffered text tier at `3544B / 3334B`. `IMAGE_TILED_RLE_565_0` stays outside this set because the wider hint exceeds the current perf gate on that scene.
- The older all-scenes `48px` horizontal walk used a `192 tail cols * 16 rows * (2B RGB565 + 1B alpha8) = 9216B` codec tail cache. That geometry is no longer the current shipped codec-alpha hotspot after the scene-selective `96x8` hint, but it remains the baseline behind the rejected tail-cap A/Bs below. The optional `96B` round-rect/circle row caches remain compiled out by default.
- 2026-03-29 cap experiments confirmed that, on that older all-scenes `48px` horizontal walk, the `192` tail was already the minimum single-pass width under `240px` screen width: `EGUI_CONFIG_IMAGE_CODEC_TAIL_ROW_CACHE_MAX_COLS=144` regresses `IMAGE_QOI_565_8 2.133 -> 8.887`, `EXTERN_IMAGE_QOI_565_8 2.565 -> 12.507`, `IMAGE_RLE_565_8 1.401 -> 3.234`, `EXTERN_IMAGE_RLE_565_8 2.222 -> 7.961`; `96` is even worse at `8.677 / 12.398 / 3.376 / 8.034`. The accepted scene-selective `96x8` shipped path changes the live hotspot numbers above, but it does not rescue these rejected narrower-tail caps for the old all-scenes geometry.
- A second 2026-03-29 A/B round also checked `176` and `184` tail caps with `EGUI_CONFIG_IMAGE_QOI_CHECKPOINT_COUNT=1/2` plus `EGUI_CONFIG_IMAGE_RLE_CHECKPOINT_ENABLE=1`, against the current `d3d37bf` baseline (`IMAGE_QOI_565_8 2.258`, `EXTERN_IMAGE_QOI_565_8 2.734`, `IMAGE_RLE_565_8 1.561`, `EXTERN_IMAGE_RLE_565_8 2.385`). All four variants still regress by `+45% ~ +66%`, so they are rejected before any further heap follow-up.
- Geometry explains why the cliff persists: with `240px` image width and a `48px` horizontal PFB walk, the first visible tile consumes columns `0..47`, while later neighbors need the union `48..239`, which is exactly `192` columns. Any single-pass tail cache below `192` must miss the last tile and force another row-band decode; row-band checkpoints only restore band-start state, and do not change that coverage requirement. This is an inference from the current walk order, and the measurements below confirm it.

| Config | `IMAGE_QOI_565_8` | `EXTERN_IMAGE_QOI_565_8` | `IMAGE_RLE_565_8` | `EXTERN_IMAGE_RLE_565_8` | Verdict |
| --- | ---: | ---: | ---: | ---: | --- |
| Historical baseline (`d3d37bf`) | `2.258` | `2.734` | `1.561` | `2.385` | Baseline before the later frame-heap decoder-state move |
| `tail=176, qoi_cp=1, rle_cp=1` | `3.616 (+60.14%)` | `4.536 (+65.91%)` | `2.270 (+45.42%)` | `3.911 (+63.98%)` | Reject |
| `tail=176, qoi_cp=2, rle_cp=1` | `3.617 (+60.19%)` | `4.537 (+65.95%)` | `2.270 (+45.42%)` | `3.911 (+63.98%)` | Reject |
| `tail=184, qoi_cp=1, rle_cp=1` | `3.629 (+60.72%)` | `4.544 (+66.20%)` | `2.284 (+46.32%)` | `3.927 (+64.65%)` | Reject |
| `tail=184, qoi_cp=2, rle_cp=1` | `3.630 (+60.76%)` | `4.544 (+66.20%)` | `2.284 (+46.32%)` | `3.927 (+64.65%)` | Reject |

- Inferred from the same geometry, `176` and `184` only remove `16` / `8` columns from the `192`-column tail cache, so the raw tail payload saving before checkpoint overhead is only `768B` / `384B`. With the measured `+45% ~ +66%` perf cliff, these variants do not justify additional heap measurements for the default path.

### Rejected Sparse Alpha Tail Cache A/B

2026-03-29 also tried a `tail alpha -> sparse row table` direction for the current `192`-column codec tail cache, with `-DEGUI_CONFIG_IMAGE_CODEC_TAIL_ALPHA_SPARSE_ENABLE=1`. The idea was to keep tail RGB565 raw, but pack alpha8 rows into a sparse `nonzero_bits + partial_bits + partial_values` form because the `star` alpha assets are mostly `0/255`. These measurements were taken before the later `qoi_state/rle_state -> frame heap` follow-up, so the table below isolates the row-cache part and does not include the extra transient `208B` / `16B` decoder state now present in the current baseline.

| Config | `IMAGE_QOI_565_8` | `EXTERN_IMAGE_QOI_565_8` | `IMAGE_RLE_565_8` | `EXTERN_IMAGE_RLE_565_8` | Heap peak | alloc/free | Verdict |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | --- |
| Historical default | `2.264` | `2.740` | `1.572` | `2.395` | `9360B` | `32 / 32` | Baseline before the later frame-heap decoder-state move |
| `tail_alpha_sparse=1` | `8.988 (+297.00%)` | `9.464 (+245.40%)` | `8.296 (+427.74%)` | `9.119 (+280.75%)` | `7512B` | `376 / 376` | Reject |

- This sparse-alpha variant does cut the four compressed alpha hotspots from `9360B` down to `7512B`, i.e. `-1848B`, which is larger than the current `>500B => 10%` RAM/perf trade gate.
- However the hit-path expansion and per-row sparse packing cost is far beyond the acceptable line: the measured regressions are `+245% ~ +428%`, and alloc/free traffic jumps from `32 / 32` to `376 / 376`.
- Because this is a default-path codec regression, not a small-scene corner case, the implementation is rejected and not kept in-tree. On the current shipped baseline, the largest verified compressed-image hotspots are `IMAGE_TILED_QOI_565_8` at `5008B`, `IMAGE_TILED_RLE_565_8` at `4816B`, `IMAGE_RLE_565_8` / `EXTERN_IMAGE_RLE_565_8` at `3760B`, and `IMAGE_QOI_565_8` / `EXTERN_IMAGE_QOI_565_8` / `MASK_IMAGE_QOI_8_ROUND_RECT` / `EXTERN_MASK_IMAGE_QOI_8_ROUND_RECT` at `3664B`.

### Rejected Global Logical PFB Probe A/B

These A/Bs keep the physical `PFB` bytes unchanged and only reinterpret logical tile width during the refresh walk. After the latest follow-up, the shipped path now accepts only a scene-selective logical-width hint for the current high-heap codec scenes; the global or all-scenes probe shapes below remain rejected unless they pass manual comparison against `doc/source/performance/perf_report.md`.

| Config | Whole-run heap peak | Main perf result | Verdict |
| --- | ---: | --- | --- |
| all codec scenes use logical `64x12` tiles | `6736B` (`-2832B`) | QOI alpha scenes stay around `+2.11% / +6.23% / +1.46% / +6.98%`, but internal RLE scenes cross the `>500B => 10%` line: `IMAGE_RLE_565 +16.26%`, `IMAGE_RLE_565_8 +12.92%`; external RLE also regresses `+5.59% / +8.24%` | Reject |
| QOI-only logical `64x12` tiles | `9376B` (`-192B`) | Savings are only `192B`, but `IMAGE_QOI_565_8` / `EXTERN_IMAGE_QOI_565_8` still regress around `+6% ~ +7%`, which misses the `<=500B => 5%` line | Reject |
| all codec logical `64x12` + `EGUI_CONFIG_IMAGE_RLE_CHECKPOINT_ENABLE=1` | `6736B` (`-2832B`) | RLE checkpoint changes stay in noise for this probe and do not recover the regressions above | Reject |

- `MASK_IMAGE_TEST_PERF_ROUND_RECT` and related masked round-rect scenes did improve under the wider logical tiles, but shipped defaults must satisfy the whole-scene RAM/perf gate rather than one hotspot.
- `python scripts/code_perf_check.py` only reports that the run completed; it does not compare against the historical baseline. These probe experiments were accepted or rejected only after manual comparison with `doc/source/performance/perf_report.md`.
- The retained outcome is no longer just the measurement tooling: the shipped path now keeps the core logical-width infrastructure plus a `HelloPerformance` scene-selective hint that returns `96` for the current high-heap codec scenes and `0` for all other scenes. Non-hotspot scenes therefore still use the original `48x16` walk.
- A 2026-03-30 follow-up fixed the probe-only resize-map assert so logical widths above the physical `48px` tile can finish. That lets wider `96x8 / 128x6 / 192x4` probe shapes complete, but they still fail the default gate and are rejected before any new heap headline is adopted.

| Config | Whole-run heap peak | Main perf result | Verdict |
| --- | ---: | --- | --- |
| all scenes logical `96x8` tiles | not rerun after rejection | Targeted codec scenes mostly stay in range (`IMAGE_QOI_565_8 -5.32%`, `EXTERN_IMAGE_QOI_565_8 -4.51%`, `IMAGE_RLE_565 +8.80%`, `IMAGE_RLE_565_8 +1.91%`), but the full-scene compare fails badly on non-codec paths: `TEXT_ROTATE_BUFFERED +33.13%`, `TEXT +17.51%`, `CIRCLE_FILL +25.29%`, `ANIMATION_TRANSLATE +15.65%`, `IMAGE_TILED_565_0 +10.06%` | Reject |
| logical `128x6` tiles | not rerun after rejection | Wider-tile spot checks already cross the gate on active codec/mask scenes: `IMAGE_QOI_565_8 -10.22%`, `EXTERN_IMAGE_QOI_565_8 -8.63%`, `IMAGE_RLE_565 +11.07%`, `MASK_IMAGE_QOI_8_ROUND_RECT +6.65%`, `EXTERN_MASK_IMAGE_QOI_8_ROUND_RECT +8.98%` | Reject |
| logical `192x4` tiles | not rerun after rejection | The most aggressive width is clearly worse on the same spot checks: `IMAGE_RLE_565 +25.52%`, `IMAGE_RLE_565_8 +8.68%`, `EXTERN_IMAGE_RLE_565 +9.27%`, `MASK_IMAGE_TEST_PERF_ROUND_RECT +14.11%`, `MASK_IMAGE_QOI_8_ROUND_RECT +17.83%`, `EXTERN_MASK_IMAGE_QOI_8_ROUND_RECT +20.21%` | Reject |

- These three wider-shape A/Bs were rejected on performance before a new whole-run heap rerun, because the shipped default must pass the full benchmark gate rather than only the codec hotspot subset.
- The accepted shipped change is narrower than those rejected probes: only the selected high-heap codec scenes reuse a logical `96x8` walk, and the latest follow-up extends that shipped set beyond the earlier codec-alpha subset while still keeping `IMAGE_TILED_RLE_565_0` excluded. The current shipped path drops whole-run heap `6448 -> 5008` on top of `cfd723e` while keeping the full benchmark inside the `>500B => 10%` rule. That accepted path is not the same as the rejected all-scenes `96x8` probe above.

- The latest buffered-text rounds trim `TEXT_ROTATE_BUFFERED` further from `4560B` to `3334B`: first the visible-alpha8 ceiling moved `3648 -> 3072`, then the visible glyph list moved from pointers to compact layout indices, and the latest heap-cliff audit proved `2560B` is now the smallest accepted default.
- The current next-largest verified non-codec hotspot is now `EXTERN_TEXT_ROTATE_BUFFERED` at `3544B`; the remaining gap above the internal path stays `210B` after the existing `14`-row external glyph chunk streaming and the latest visible-list / ceiling follow-up.
- The default external raw-image path now enforces the `<= 2 rows/cols` heap ceiling as well: the shared row caches move from `1920/960` down to `960/480`, so representative external raw-image scene-local heap drops to `1440B` (`EXTERN_IMAGE_565_8`, `EXTERN_IMAGE_ROTATE_565_8`) or `1536B` (`EXTERN_IMAGE_RESIZE_565_8` with its extra `src_x_map`).
- This round is a policy-enforcement tradeoff rather than a perf-neutral win: the prior `1920/960` default exceeded the current HelloPerformance row/column ceiling, so the shipped low-RAM default now accepts some external raw-image slowdown. The worst verified cases are `EXTERN_IMAGE_565_1 +13.11%`, `EXTERN_IMAGE_ROTATE_565_1 +12.56%`, and `EXTERN_IMAGE_ROTATE_TILED_565_8 +12.26%` relative to the previous `4`-row default.
- The latest transform-side follow-up then recovers a good part of that rotate-only regression without adding RAM: aligned chunk reloads leave `EXTERN_IMAGE_565_8` and `EXTERN_IMAGE_RESIZE_565_8` unchanged, while representative rotate cases improve to `EXTERN_IMAGE_ROTATE_565_1 14.696 -> 13.474 (-8.32%)`, `EXTERN_IMAGE_ROTATE_565_2 15.508 -> 14.106 (-9.04%)`, `EXTERN_IMAGE_ROTATE_TILED_565_0 11.707 -> 10.630 (-9.20%)`, and `EXTERN_IMAGE_ROTATE_TILED_565_8 17.633 -> 16.029 (-9.10%)`.
- A 2026-03-29 follow-up also checked a stricter `1`-row external raw-image cache (`480B` data + `240B` alpha). That does halve the representative scene-local heap again to `720B` (`EXTERN_IMAGE_565_8`, `EXTERN_IMAGE_ROTATE_565_8`) or `816B` (`EXTERN_IMAGE_RESIZE_565_8`), but the whole-run heap headline still stays pinned by codec scenes, while direct-draw and rotate regress too far: `EXTERN_IMAGE_565_1 1.458 -> 1.795 (+23.11%)`, `EXTERN_IMAGE_565_8 1.630 -> 1.967 (+20.67%)`, `EXTERN_IMAGE_ROTATE_565_1 13.474 -> 15.161 (+12.52%)`, and `EXTERN_IMAGE_ROTATE_565_8 15.875 -> 19.655 (+23.81%)`.
- Because that `1`-row experiment saves `>500B` but misses the `10%` perf gate on several active scenes, it stays rejected for the shipped default. The current `2`-row external cache remains the lowest accepted default point, while the `1`-row variant is left as measurement-only via `USER_CFLAGS`.
- Even after cutting both buffered text scenes below `4.3KB`, the dominant heap headline is still controlled by compressed-image row caching rather than text.
- Further default heap reduction below this codec floor now requires a different architecture, such as a different PFB walk or finer-grained decoder checkpoints. Simply shrinking tail columns is no longer a viable default optimization path.

### Rejected Whole-Image HQ Cache Direction

The earlier external whole-image resident-cache experiments are now explicitly rejected for HelloPerformance, even when they improve performance:

| Direction | Resident heap | Measured speedup | Status |
| --- | ---: | ---: | --- |
| `120x120 RGB565 + alpha8` external whole-image cache | `43200B` | `3.17x` on `EXTERN_IMAGE_ROTATE_565_8` | Rejected: exceeds the allowed `1 * PFB` / `2` rows-or-columns ceiling |
| `40x40 RGB565` external whole-image cache | `3200B` | `2.05x` on `EXTERN_IMAGE_ROTATE_TILED_565_0` | Rejected: still whole-image resident heap |
| `40x40 RGB565 + alpha8` external whole-image cache | `4800B` | `2.40x` on `EXTERN_IMAGE_ROTATE_TILED_565_8` | Rejected: still whole-image resident heap |

- Going forward, external image draw/transform on HelloPerformance must stay with row-cache or row-scratch style solutions only.
- If a higher-performance variant is proposed later, it still must stay within at most `1 * PFB` bytes, or at most `2` image/screen-sized rows or columns of heap scratch/cache.

### Stack Measurement

| Function | File | Frame (B) | Relevance | Notes |
| --- | --- | ---: | --- | --- |
| `egui_view_heart_rate_on_draw` | `src/widget/egui_view_heart_rate.c` | 1200 | Compiled hotspot, but discarded from the final HelloPerformance image | Large locals: `points[240]`, `raw_vals[120]`, `smooth_vals[120]`; `output/main.map` lists this section only under `Discarded input sections` |
| `egui_view_virtual_tree_walk_internal` | `src/widget/egui_view_virtual_tree.c` | 1112 | Compiled hotspot, but discarded from the final HelloPerformance image | Large local stack frame: `frames[EGUI_VIEW_VIRTUAL_TREE_MAX_DEPTH]`; also only appears under `Discarded input sections` |
| `line_hq_draw_polyline_segment` | `src/core/egui_canvas_line_hq.c` | 976 | Compiled hotspot, but discarded from the final HelloPerformance image | The polyline helper is not linked into the current benchmark image; `output/main.map` shows it only in the discarded section list |
| `egui_canvas_draw_thick_line_scan` | `src/core/egui_canvas_line.c` | 432 | Current largest linked HelloPerformance frame | The scratch is scalar-only and size-independent; this path is now the top active stack hotspot in the linked image |
| `egui_canvas_draw_circle_fill_gradient` | `src/core/egui_canvas_gradient.c` | 424 | HelloPerformance gradient circle scenes | The fixed LUT remains size-independent; this is now the second active stack hotspot |
| `egui_canvas_draw_polygon_fill_gradient` | `src/core/egui_canvas_gradient.c` | 408 | HelloPerformance polygon-fill gradient scenes | Packed edge/intersection scratch removes another `128B` from the former `536B` hotspot without changing buffer placement |
| `egui_canvas_draw_polygon_fill` | `src/core/egui_canvas_polygon.c` | 408 | HelloPerformance polygon-fill scenes | Same packed scratch treatment as the gradient path; active frame stays well below the current reserve |
| `egui_image_rle_draw_image` | `src/image/egui_image_rle.c` | 392 | HelloPerformance compressed-image RLE scenes | Current low-RAM RLE path still carries row-cache state and visible-span control flow, but no size-related local arrays |
| `egui_canvas_draw_triangle_fill` | `src/core/egui_canvas_triangle.c` | 248 | HelloPerformance triangle-fill scenes | Function-local `optimize("Os")` trims the former `504B` frame, so triangle fill no longer drives the active reserve budget |
| `egui_canvas_draw_round_rectangle_corners_fill_gradient` | `src/core/egui_canvas_gradient.c` | 192 | HelloPerformance gradient round-rect scenes | Function-local `optimize("Os")` cuts the former `472B` frame while the same code path also gets faster in QEMU |
| `egui_view_gauge_on_draw` | `src/widget/egui_view_gauge.c` | 88 | Linked benchmark scene | Gradient/text drawing moved into small helpers, so the app-side draw frame is no longer a hotspot |
| `egui_canvas_draw_rectangle_fill_gradient` | `src/core/egui_canvas_gradient.c` | 416 | HelloPerformance gradient rectangle scenes | Function-local `optimize("Os")` trims the frame while keeping the fixed LUT size-independent |
| `egui_image_std_set_image_resize_rgb565_8_common` | `src/image/egui_image_std.c` | 384 | HelloPerformance image resize + alpha path | Former `src_x_map[EGUI_CONFIG_PFB_WIDTH]` now uses heap; active frame is scalar-only plus small state |
| `egui_image_qoi_draw_image` | `src/image/egui_image_qoi.c` | 288 | HelloPerformance compressed-image QOI scenes | Unmasked QOI rows still direct-blend the first visible alpha span, while masked rows now receive caller-owned transient heap scratch, so the frame stays scalar plus bounded control state |
| `egui_canvas_draw_text_transform` | `src/core/egui_canvas_transform.c` | 368 | HelloPerformance rotated-text runtime path | Function-local `optimize("Os")` plus the latest visible-list scratch simplification keep this frame at `368B`, well below the current linked stack peak |
| `egui_image_std_set_image_resize_rgb565` | `src/image/egui_image_std.c` | 328 | HelloPerformance image resize rgb565 path | Resize `src_x_map` and round-rect row cache now allocate from heap by draw width / tile height |
| `egui_canvas_draw_image_transform` | `src/core/egui_canvas_transform.c` | 344 | HelloPerformance image rotate scenes | Function-local `optimize("Os")` trims the transform path; the rejected whole-image cache hook remains removed |
| `egui_canvas_draw_line_round_cap_hq` | `src/core/egui_canvas_line_hq.c` | 280 | HelloPerformance line round-cap scenes | Function-local `optimize("Os")` trims the active round-cap helper from the former `744B` level |
| `egui_canvas_draw_triangle_fill_gradient` | `src/core/egui_canvas_gradient.c` | 280 | HelloPerformance gradient triangle scenes | Function-local `optimize("Os")` trims the frame and `GRADIENT_TRIANGLE` also improves in the latest QEMU A/B |
| `egui_canvas_draw_line_hq` | `src/core/egui_canvas_line_hq.c` | 288 | Linked HelloPerformance line HQ scenes | Function-local `optimize("Os")` trims the former `824B` frame to `288B` without tripping the current `5%` perf gate |
| `egui_image_qoi_decode_row_rgb565_alpha8_masked_split` | `src/image/egui_image_qoi.c` | 56 | Current QOI masked round-rect helper | Visible RGB565/alpha scratch now comes from caller-owned transient heap buffers sized by the current visible span, so the helper no longer carries fixed `48px` local arrays |
| `egui_shadow_draw_corner` | `src/shadow/egui_shadow.c` | 176 | HelloPerformance shadow scenes | Function-local `optimize("Os")` trims the current corner helper and `SHADOW_ROUND` also improves in the latest QEMU A/B |
| `text_transform_draw_visible_alpha8_tile_layout` | `src/core/egui_canvas_transform.c` | 128 | HelloPerformance rotated-text helper | Former `4352B` stack scratch removed; visible alpha8 tile now allocates from heap according to actual transformed bounds |
| `text_transform_rasterize_visible_alpha8_layout` | `src/core/egui_canvas_transform.c` | 96 | HelloPerformance external rotated-text visible-tile helper | External visible-tile glyph scratch moved from local arrays to transient heap as a `14`-row glyph chunk stream sized by actual glyph row bytes |
| `egui_font_std_draw_single_char_desc_external_stream` | `src/font/egui_font_std.c` | 88 | HelloPerformance external font draw path | Removed former `pixel_buf[256]`; full glyph bitmap now loads into heap scratch, reused by `fast_4` / `fast_4_mask` string draw calls |

- The raw `.su` report still contains `1200B`, `1112B`, and `976B` frames, but `output/main.map` shows all three functions only under `Discarded input sections` for the current HelloPerformance image.
- The current largest linked HelloPerformance frame is now `egui_canvas_draw_thick_line_scan (432B)`; the next linked hotspots are `egui_canvas_draw_circle_fill_gradient (424B)`, `egui_canvas_draw_rectangle_fill_gradient (416B)`, and the polygon fill pair (`408B` each), and there is still no linked function frame `>= 1KB` in the final benchmark image.
- The HelloPerformance QEMU linker reserve is now `432B` via `__qemu_min_stack_size__=0x01b0`; the latest rotated-text visible-list round keeps that reserve unchanged, and this current state is backed by the latest `.su`/`main.map` cross-check plus runtime `223/223`, unit test `649/649`, and `python scripts/code_perf_check.py --profile cortex-m3 --threshold 10`.
- The rejected 2026-03-30 `0x0180` stack-reserve probe is worth keeping in mind for future work: even if `egui_canvas_draw_thick_line_scan()` is split down to `384B`, the active linked ceiling is still pinned above that by the current gradient/polygon/RLE cluster (`424B / 416B / 408B / 408B / 392B`), so further reserve work is not worthwhile unless those frames move together.
- The rotated-text stack cleanup now happens in three steps: moving layout/tile collectors to transient heap first cut `egui_canvas_draw_text_transform` from `2920B` to `760B`, the earlier function-local `optimize("Os")` round cut it again to `376B`, and the latest visible-list scratch simplification trims it slightly further to `368B` without tripping the current perf gate.
- The visible alpha8 tile scratch, direct-draw external font scanline cache, visible-tile `14`-row glyph chunk scratch, and direct-draw glyph pixel scratch all remain heap-backed, which is the required placement for these font-size-related buffers.
- The image resize / round-rect scratch cleanup still caps the modified image frames at `384B` (`egui_image_std_set_image_resize_rgb565_8_common`) and `328B` (`egui_image_std_set_image_resize_rgb565`); the former `PFB_WIDTH` / `PFB_HEIGHT` sized locals are gone.
- The latest function-local `optimize("Os")` round also trims `egui_canvas_draw_image_transform` to `344B`, `egui_canvas_draw_line_round_cap_hq` to `280B`, and `egui_shadow_draw_corner` to `176B`, while the verified perf profile stays green and several geometry/shadow scenes get faster.
- The circle-mask row-cache cleanup still keeps the modified benchmark entry frames at only `72B` (`egui_view_test_performance_test_mask_rect_fill_circle`) and `64B` (`egui_view_test_performance_test_mask_image_circle`, `egui_view_test_performance_test_mask_image_test_perf_circle`, `egui_view_test_performance_test_extern_mask_image_circle`, `egui_view_test_performance_test_extern_mask_image_test_perf_circle`).
- The latest codec visible-row refinement still does not introduce a new large stack hotspot: `egui_image_qoi_draw_image` is `288B`, `egui_image_rle_draw_image` is `392B`, `egui_image_qoi_decode_row_rgb565_alpha8_masked_split` is now only `56B`, `egui_image_qoi_decode_pixels_rgb565_alpha8_blend_external_partial` is `224B`, `egui_image_qoi_decode_pixels_rgb565_alpha8_blend_partial` is `80B`, `egui_image_qoi_decode_row_rgb565_alpha8_blend_split` is `32B`, and `egui_image_qoi_blend_repeat_rgb565_alpha8_fast` is `32B`.
- The logical-PFB related helpers also stay tiny on stack: core-side `egui_core_get_logical_pfb_target_width_hint` is `4B`, `egui_core_get_logical_pfb_probe_width` is `32B`, and `egui_core_apply_logical_pfb_probe_shape` is `32B`; the new `HelloPerformance` app-side helper `egui_view_test_performance_uses_logical_pfb_96_hint()` is `16B`, and the override `egui_core_get_logical_pfb_target_width_hint()` in `uicode.c` is `8B`. None of these change the active stack budget.
- The latest round keeps the user-configurable `pfb` pixel buffer unchanged and moves pressure from stack to transient heap instead of fixed static RAM.

### RLE Checkpoint A/B

Same code, only toggle `EGUI_CONFIG_IMAGE_RLE_CHECKPOINT_ENABLE`.

| Test | checkpoint off (ms) | checkpoint on (ms) | Delta |
| --- | ---: | ---: | ---: |
| `MASK_IMAGE_RLE_NO_MASK` | 1.538 | 1.539 | +0.1% |
| `EXTERN_MASK_IMAGE_RLE_ROUND_RECT` | 5.712 | 5.713 | +0.0% |
| `IMAGE_TILED_RLE_565_0` | 0.940 | 0.939 | -0.1% |

- The measured impact stays in noise range (`<= 0.1%`), so keeping the `16B` RAM saving is justified.
